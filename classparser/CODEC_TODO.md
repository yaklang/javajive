# JavaJive 反编译器正确性账本 (CODEC_TODO)

> **北极星**: 复杂 JAR 做到「反编译 → `javac` 重编译 → 重新打包成 jar → 被外部 JVM 加载/校验/调用」全链路正确。
>
> - 度量口径与长尾清零方法: 见根目录 [`HARNESS.md`](../HARNESS.md)。
> - 当前可执行的缺陷工单(选靶 + 复现命令): 见根目录 [`TODO.md`](../TODO.md)。
> - 本文件只登记**当前真实状态 + 已治本项 + 剩余缺陷**。所有数字均由 harness 真实跑出(`javac 17.0.12`, 本机 `~/.m2`), 禁止估算或编造; 绝对值随 JDK / jar 版本浮动, 以 delta 与趋势为准。

---

## 0. 为什么用整树(tree)口径, 而不是逐文件(iso)

反编译器把嵌套类发成**独立的扁平单元** `Outer$Inner.java`(见 `dumper.go` 架构注释)。这带来两种度量:

- **tree(整树)**: 所有扁平单元一起 `javac`(依赖在 classpath)。兄弟扁平 `$` 引用互相解析得到, 产物可直接重打包。**这是「能否重打包」的真口径。**
- **iso(逐文件)**: 每个单元单独 `javac`(原始 jar + 依赖在 classpath)。`Outer$Inner` 这种扁平 `$` 类型引用在原始 jar 里按源名 `Outer.Inner` 索引、解析不到, 于是报海量 `cannot find symbol`/`private access`。**这是 iso 口径的系统性假阳性, 不是反编译缺陷, 也不阻碍重打包**(详见 §4)。

> 结论: 治本与验收以 **tree errLines / blockerUnits** 为准; iso 仅用于侧写、对照, 不作为重打包准绳。

---

## 1. 当前真实状态(4 个真实 JAR)

| jar | units | tree errLines | tree blockerUnits | iso fails | 重打包(repackage)状态 |
|---|---|---|---|---|---|
| **codec** (commons-codec 1.15) | 106 | **0** | **0** | 38 | ✅ **全链路达标** |
| **spring** (spring-core 5.3.27) | 974 | **2** | **1** | 384 | ⚠ 仅 1 个合成内部类阻断 |
| **fastjson2** (2.0.43) | 681 | **52** | 23 | 342 | ⚠ 泛型擦除为主 |
| **guava** (28.2-android) | 1825 | **178** | 118 | 1149 | ⚠ 泛型擦除/边界为主 |

> 另: **gson**(非 §1 四大往返 jar)经 dup2 多临时拼接 + 通配符返回/字段造型 + 扁平内部类外层形参元数对齐 + null 链式赋值临时抑制 + `NATURAL_ORDER` 构造器实参造型 + try/catch 兄弟臂 boolean phi 合并, 一度报告 **tree decErr=1**。**勘误(本轮)**: 那个 `decErr=1` 是 `stream/JsonWriter` 的 `continue` 丢失错误**遮蔽**了下游(`javac` 遇首个流分析错即停, 后续文件的定值流分析被跳过)——并非「LinkedTreeMap 实际可编译」, 之前注记有误。逐文件(iso)口径始终显示 gson 约 45 个真实失败文件, 证实存在被遮蔽的深层缺陷。
>
> 本轮按**解遮蔽链**逐个治本(均带 kill-switch + iso A/B 零回归, 见 §2 顶部五行):
> 1. `JsonWriter.string` 分裂-continue 丢失 → `convertSplitContinueToLatch`(`JDEC_SPLIT_CONTINUE_LATCH_OFF`), 解遮蔽出 ↓
> 2. `JsonAdapterAnnotationTypeAdapterFactory:46` 子类型臂 phi → `reachingRefSlotSubtypeArmMerge`(`JDEC_REF_SLOT_SUBTYPE_ARM_MERGE_OFF`), 解遮蔽出 ↓
> 3. `LinkedTreeMap.find:82` 同槽不相交活跃区别名拆分 → prebind minted-reuse 守卫(`JDEC_IFELSE_PREBIND_MINTED_REUSE_OFF`), 解遮蔽出 ↓
> 4. `LinkedTreeMap.rebalance` 循环归纳变量 raw-vs-参数化误拆 → `AssignVarGuarded` 同擦除复用(`JDEC_RAW_GENERIC_SLOT_MERGE_OFF`), 解遮蔽出 ↓
> 5. `JsonStreamParser.hasNext` synchronized 块 body(本体即 try/catch)被丢成空块 → `removeSunkMonitorExit`(`JDEC_SYNC_NESTED_MONITOREXIT_OFF`)。
>
> ✅ **gson tree decErr=0(已清零, units=183)**——LinkedTreeMap / JsonStreamParser 全清, 整 jar `javac --release 8` 重编译零错误, 可直接重打包。计数 `=1`→`=5`→`=1`→`=0` 全程系解遮蔽+逐个治本, 非回归(iso 同步 45→44, fastjson2/guava iso 全程单调不增)。验收: `PROFILE_JAR=gson RECOMPILE_MODE=tree` → decErr=0 + 确定性 `TestZZDeterminism`(195 单元逐字节一致)+ `test/cross` 全套零回归。
> 至此 **§1 四大往返 jar 之外、gson 成为继 codec 后第二个 tree 清零库**; 长尾仅剩 spring(2)/fastjson2(52)/guava(178)。

**codec 已完整证明北极星**(承重于 `test/cross/jar_roundtrip_test.go` 的 `TestJarRoundTripRepackage/codec`):
`decompile → javac 重编译(0 error) → archive/zip 重打包 → java -Xverify:all 逐类加载校验 107/107 通过 → 调用差分(Base64 / Hex / MD5 / SHA-256)与原始 jar 逐字节一致`。

> CI 常驻承重: `TestSyntheticJarRoundTrip`(无需 `~/.m2`)对一个含枚举+switch / 泛型 / lambda / varargs / try-catch 的多类程序跑完整往返, 断言运行输出逐字节一致 + 全类 verify。它守住「往返能力」永不回归。

---

## 2. 本轮已治本(每项: kill-switch + 承重测试 + 回归种子 + 实测 delta)

| 缺陷 | 根因 / 治法 | kill-switch | 实测 |
|---|---|---|---|
| **类型变量数组的元素存储 `this.buf[i] = objExpr`(字段声明 `T[]`/`K[]`/`V[][]`)漏补 `(T)` 下行造型(治本, 跨库「Object→类型变量造型缺失」族·LHS 数组元素侧)** | `Object cannot be converted to T/K/V/E`(guava `TopKSelector.buffer[i]` 2 行 + `HashBiMap.keys[i]/values[i]` 2 行 + `DenseImmutableTable.values[r][c]`(2D)1 行 + `ImmutableSortedMultiset$SerializedForm.elements[i]` 1 行, 共 6 行): 源码如 `T[] buffer; ...; buffer[i] = (T) value;`——数组分量类型是类型变量 T, 流入的值是擦除后的 `Object`(raw `Iterator.next()`/`Map.Entry.getValue()`/另一擦除字段读), 源必带 unchecked `(T)`, 但 `aastore` 把它擦除成 no-op(T 擦除到 Object 上界); javac 按声明 `T[]` 复查 → `Object cannot be converted to T`。元素存储 LHS 是 `JavaArrayMember`(非 `RefMember`), 既有 `typeVarFieldStoreCast`(整字段存储)的 switch 跳过它; 而值的 `member.Type()` 常是擦除 `Object`(字段访问只带擦除描述符), 无法据此判类型变量。治法: 新增 `typeVarArrayElementStoreCast`(`typeVarFieldStoreCast` 的数组元素姊妹, expression.go `typeVarArrayArgCast` 的 LHS 对偶)——展开嵌套 `JavaArrayMember` 数索引深度、定位**同类字段基**(`this.f`/`CurrentClass.f`), 用字段**已录的泛型 Signature**(`FieldTypeVar`, 如 `keys→K[]`、`values→V[][]`)取元素类型变量(与 dumper 实际声明逐字一致, 而非擦除的 `member.Type()`), 当**索引维数==字段数组维度**(部分索引留下数组型变量 `V[]` 不补标量造型)、元素是可命名类型参数(`FieldTypeVar` 录入时已 `IsTypeParam` 守门)、且值为非 null 非基元、其渲染类型≠该变量时, 补出 `buf[i] = (T) value`。安全核心: 元素类型变量取自字段 Signature(与声明一致, 不依赖可能擦除的值类型); `(T)` 仅当 T 在当前类是类型参数(可命名可编译)时发出; 引用数组元素的 `(T)` 是合法 unchecked cast(运行时擦除为 no-op); 仅限同类字段数组基(局部数组基出作用域, 不触碰); 维数严格匹配杜绝把整子数组误造型成标量 | `JDEC_TYPEVAR_ARRAY_ELEM_STORE_CAST_OFF` | **guava tree 184→178**(delta -6, A/B 集合 diff: 仅 `TopKSelector`(T×2)+`HashBiMap`(K,V)+`DenseImmutableTable`(V)+`ImmutableSortedMultiset$SerializedForm`(E)6 行消失, **新增回归 0 行**; `incompatible types` 桶 132→126, blockerUnits 120→118), fastjson2 52=52(无此形态)/ gson 0=0 **delta 全 +0(零回归)**。承重 `TestTypeVarArrayElementStoreCastIsLoadBearing`(真 jar 上述 4 类单编, ON=0 / OFF=4), 全量 23 个 `IsLoadBearing` 承重测试零回归 + 确定性 `TestZZDeterminism`(guava 1892 单元两次反编译逐字节一致)+ classparser 全套单测零回归 |
| **调用点形参被泛型解析器复原成「作用域内类型变量 T」、实参不可直赋(Object 标量 / 引用数组)时漏补 `(T)` 下行造型(治本, 跨库「Object→类型变量造型缺失」族, 两条互补缺口)** | `Object cannot be converted to T`(guava `Comparators.isInOrder/isInStrictOrder` 25/46 共 2 行) + `Object[] cannot be converted to T`(fastjson2 `CSVReaderUTF8/UTF16.readLineObjectAll` 589/544 共 2 行)。**缺口①(JDK 参数表缺 `Comparator.compare`)**: 源码 `Iterator<? extends T> it = iterable.iterator(); T prev = it.next(); ...; if(comparator.compare(prev, next) > 0) ...`——`iterable.iterator()` 的返回 `Iterator<? extends T>` 因 `InstantiateJDKMethodReturn` 的**通配符守卫**退化为 raw `Iterator`, `next()` 遂给 `Object`, `compare` 描述符把两形参擦除成 `Object`; javac 按 `compare(T,T)` 复解析报 `Object→T`。`instantiatedParamType`→`receiverParamTypeArgs` 本能从 `Comparator<T>` 接收者取出类型实参 `[T]`, 但 `jdkMethodParamTypeArgIndex` **表中无 `Comparator.compare`**, 形参停在擦除 `Object`、被 924 行 `!= "java.lang.Object"` 闸门挡掉无造型。治法: 把 `Comparator.compare`(`compare(T,T)`, 单类型实参共享两形参, argc=2/ntype=1)纳入 `jdkMethodParamTypeArgIndex`——与既有 Consumer/Function/Predicate/Map/Collection 条目同构、JDK 稳定 API、通配符实参已被 `InstantiateJDKMethodParam` 守门。形参解析回 `T` 后, 既有 arg-cast 逻辑补出 `compare((T)prev,(T)next)`。**缺口②(数组实参闸 `typeVarArrayArgCast`)**: 源码 `Object[] values = readLineValues(false); consumer.accept((T) values);`——`consumer` 是 `Consumer<T>`, `accept` 形参经 `Consumer.accept` 表项已解析回 `T`(`ok1=true`), 但实参 `values` 是 `Object[]`, 其 `RawType()` 是 `*JavaArrayType` 而非 `*JavaClass`(`ok2=false`), **类-类造型分支(924, 要求 `ok1&&ok2`)对数组实参永不触发**, 裸渲染 `accept(values)` → `Object[]→T`。治法: 新增 `typeVarArrayArgCast` 分支——当**解析器已复原形参为「作用域内可命名类型变量 T」**(`resolvedGeneric && IsTypeParam(expect.Name) && expect != Object`)**且实参静态类型是数组**时, 补出 `accept((T) values)`(引用数组绝不可直赋裸类型变量, 源必带 unchecked `(T)`, 其字节码擦除为 no-op——数组已满足 T 的 Object 上界)。安全核心: 两缺口均**只在泛型解析器成功复原形参为类型变量后**才补造型(绝不在 raw 描述符 `Object` 形参上补, 否则会对普通 `Object[]`/`Object` 实参过度造型); `(T)` 仅当 T 在当前作用域是类型参数(可命名、可编译)时发出; 缺口②要求 `ok1`(形参复原为裸类型变量, 非数组——若 T 被实例化为数组则 `ok1=false` 不触发, 裸实参本就正确) | `JDEC_TYPEVAR_ARRAY_ARG_CAST_OFF`(缺口②); 缺口①在伞柄 `JDEC_GENERIC_PARAM_INFER_OFF` 下(删表项即令 ON 复现) | **guava tree 186→184**(delta -2, A/B 集合 diff: 仅 `Comparators` 2 行 `Object→T` 消失, **新增回归 0 行**)**+ fastjson2 tree 54→52**(delta -2, A/B 集合 diff: 仅 `CSVReaderUTF8`(589)+`CSVReaderUTF16`(544)2 行 `Object[]→T` 消失, **新增回归 0 行**; `incompatible types` 桶 24→22, blockerUnits 25→23, **CSV 整族清零**), gson 0=0 **delta 全 +0(零回归)**。承重 `TestComparatorCompareTypeVarCastIsLoadBearing`(真 jar `Comparators` 单编, ON=0 / OFF=2)+ `TestTypeVarArrayArgCastIsLoadBearing`(真 jar `CSVReaderUTF8`+`CSVReaderUTF16` 单编, ON=0 / OFF=2), 全量 22 个 `IsLoadBearing` 承重测试零回归 + 确定性 `TestZZDeterminism`(fastjson2 681 / gson 195 单元两次反编译逐字节一致)+ classparser 全套单测零回归 |
| **同一 JVM 槽两不相交臂存协变数组(`new String[]` / `new Object[]`)被误拆, 子类型数组臂上的合流读报错(治本, disjoint 槽族「数组协变父臂合并」型)** | `Object cannot be converted to String`(fastjson2 `CSVReaderUTF8.readLineValues` 428/520 + `CSVReaderUTF16` 409 共 3 行): 源码 `Object[] values = null; if (strings) values = new String[size]; else values = new Object[size]; ...; if (values != null) { if (i < values.length) values[i] = value; } return values;`——字节码 **LVT 实证 slot 2 全程是单一 `Object[] values`**(start 57 length 1260)。`String[]` 是 `Object[]` 的协变子类型(合法赋值), 但 DFS 序里 `values = null` 的 null-init 先经 null-adopt-once 采纳 `new String[]`(槽 ref 定型 `String[]`、且 `IsNullInitialized()` 终身为真、`NullTypeAdopted()` 已置), 随后 `new Object[]` 臂存入 `current=该 String[] ref`; `AssignVarGuarded` **无数组协变规则**(int 类目/裸泛型 merge 不适用、null-adopt 已耗尽), 遂**新铸独立 `Object[]` 变量(var4)**, 合流后的 `values[i] = value` / `return values` / `.length` 全绑到 `String[]` ref → `Object cannot be converted to String`。既有 `reachingRefSlot*` 四族**全部 bail 在数组上**: `classNameOf`(`CommonSuperType`/`CrossClassDirectLUB` 的基础)对数组类型返回 false, `reachingRefSlotObjectArmMerge` 比较槽类型串与 `"java.lang.Object"`(永不等于 `"...[]"`)。治法: 新增 `reachingRefSlotArrayCovariantArmMerge`(`reachingRefSlot*` 的数组元素伴生): 当 current 与存储值**都是等维(dim==1)引用数组**、元素类型串不同、经 `arrayElemLUB`(Object 万能超类 / jar 内直接子类型 `CrossClassDirectLUB` / JDK 家族 `CommonSuperType`)求出元素 LUB、且 `slotDefPhiReachesLoad`(下游 load 同时被本 store 与 current 的 def 到达)证 phi 时, 把 current **加宽到元素 LUB 数组**(`String[] → Object[]`)并续用, 两臂遂成同一 `Object[]` 变量的普通再赋值。安全核心: (a) **刻意不门控 `IsNullInitialized`**(异于标量 ref 三族)——CSV 的 current 正是 null-adopted ref(`Val` 终身留 null 字面量), 但「current 已是**已提交数组类型**」隐式保证它绝非标量 Object null-init(标量 Object null-init 无数组类型, `ct.IsArray()` 闸门挡掉), 故 adopt-once 耗尽与否无关; (b) phi 闸门是 disjoint 判据——真不相交复用(`String[]` 量死后槽改作无关 `Object[]`)无 load 被双 def 共达, 必拆; (c) 原字节码既已编译, `String[]` 可赋 `Object[]`, 且源码里任何窄元素读必带显式 checkcast(dumper 渲染成 `(String)values[i]`), 故加宽恒类型安全; 基元数组(`int[]`/`byte[]`, 不变型无协变)与元素无可证父类型者返 nil 自然不触发 | `JDEC_REF_SLOT_ARRAY_COVARIANT_ARM_MERGE_OFF` | **fastjson2 tree 57→54**(delta -3, A/B 集合 diff: 仅 `CSVReaderUTF8`(428/520)+ `CSVReaderUTF16`(409)3 行 `Object→String` 消失, **新增回归 0 行**——残留的 `Object[]→T` 两行仅因删除 `Object[] var4` 声明致行号上移 2 而非新错, 错误文本逐字一致; `incompatible types (assignment/return)` 桶 27→24, blockerUnits 25=25 CSV 仍因 `Object[]→T` 阻断), guava 186=186 / gson 0=0 **delta 全 +0(零回归)**。承重 `TestArrayCovariantArmMergeIsLoadBearing`(真 jar `CSVReaderUTF8`+`CSVReaderUTF16` 单独编译即复现, ON=0 / OFF=3), 全量 20 个 `IsLoadBearing` 承重测试零回归 + 确定性 `determinism_test`(fastjson2/gson 回归种子两次反编译逐字节一致)+ classparser 全套单测零回归 |
| **boolean 标志(存入 `Z` 字段)复用了不相交活跃区间的 int 循环计数器槽, 被并成单一 boolean 变量致整段 int 循环用法报错(治本, disjoint 槽族「布尔字段 sink 拆分」型)** | `bad operand types '<'` + `boolean cannot be converted to int` x2 + `bad operand type boolean for unary operator '++'`(fastjson2 `JSONPathTypedMultiIndexes.<init>` 共 4 行 24/25/25/26): 源码 `int[] indexes = new int[paths.length]; for(int i=0;i<indexPaths.length;i++) indexes[i]=...; this.indexes=indexes; boolean duplicate=false; ...; if(...){duplicate=true;break;} this.duplicate=duplicate;`。字节码实证 **JVM slot 11** 先作第一个循环的 int 计数器 `i`(offset 31 `iconst_0;istore 11` … offset 60 `iinc 11`, 活区在 offset 63 后死亡), 随后 offset 72/150/169 复用为 boolean `duplicate`(`=false`/`=true`/`putfield duplicate:Z`)。`duplicate=false` 被编成 `iconst_0;istore 11`(int 范畴 0/1 字面量), `AssignVarGuarded` 视其与槽内(已死的)int 计数器 ref 同类而**续用**, 把两不相交活区并成一个变量; 槽类型随后从 `putfield ...:Z`/`!duplicate` 解析为 boolean, 于是先前 int 循环用法(`i<len` / `array[i]` / `i++`)全报错。既有 `reachingBoolReturnSlotSplit`(sink=ireturn, 仅 boolean 方法)够不到本形态——本方法是 **void `<init>`**, boolean 判据是值前向流入 **`Z` 字段** 而非 ireturn。治法: 新增 `reachingBoolFieldSlotSplit`(`reachingBoolReturnSlotSplit` 的字段-sink 姊妹): 当槽 current 是纯 int 局部、存储是 int-0/1、本 store 前向(无中途重定义)流入 `putfield`/`putstatic` 到 **boolean(`Z`)字段**(`slotStoreFeedsBooleanField`)、**且 store 与 current 的 def 不共达任一 load**(`!slotDefPhiReachesLoad`, 真不相交)、**且** current def 与本 store 不在同一到达定义 web(`slotStoreDisjointFromCurrentWeb`)时, 把 0/1 重定型为 boolean, 使 `AssignVarGuarded` 新铸一个 boolean 量, 拆开两活区。安全核心: phi 守门是关键判据——它把「真不相交的 int 计数器/boolean 标志」(无共达 load → 拆分)与「同一 boolean 变量在 try/catch 两臂都赋值后汇入同一 `putstatic Z`」(共达 load → 拒拆, 留给 `reachingBoolSiblingArmMerge`)严格区分; 加 web 不相交闸门双重保险, 故合法的 sibling-arm boolean 合并零触碰 | `JDEC_BOOL_FIELD_SLOT_SPLIT_OFF` | **fastjson2 tree 61→57**(delta -4, A/B 集合 diff: 仅 `JSONPathTypedMultiIndexes` 4 行 int/boolean 槽错消失, **新增回归 0 行**; blockerUnits 26→25), guava 186=186 / gson 0=0 **delta 全 +0(零回归)**。承重 `TestBoolFieldSlotSplitIsLoadBearing`(真 jar `JSONPathTypedMultiIndexes` 单文件即复现, ON=0 / OFF=4), 全量 18 个 `IsLoadBearing` 承重测试零回归(含姊妹 `TestBoolReturnSlotSplitIsLoadBearing` ON=0/OFF=3 不受扰)+ 确定性 `TestZZDeterminism`(fastjson2 681 单元两次反编译逐字节一致)+ 核心反编译单测零回归 |
| **同一 JVM 槽的 current 变量声明为 `java.lang.Object`(万能超类), 某不相交臂用更具体引用值重赋, 汇合处的 instanceof/造型读绑到被拆出的具体名(治本, disjoint 槽族「Object 超类臂合并」型)** | `String cannot be converted to Map/Collection`(fastjson2 `ObjectWriterAdapter.toJSONObject` 3 行 589/590/614 + `JSONPathTwoSegment` 2 行同形态): 源码 `Object fieldValue = fieldWriter.getFieldValue(object);`(字节码 LVT 实证 slot 8 全程是**一个 `Object fieldValue`**), `if(fieldClass==Date.class) fieldValue = DateUtils.format((Date)fieldValue, format);`(及 LocalDate/LocalDateTime 共 3 条件臂, `DateUtils.format` **返回 String**), 汇合处 `if(fieldValue instanceof Map) jsonObject.putAll((Map)fieldValue); else if(Collection.class.isAssignableFrom(fieldClass)...) c = (Collection)fieldValue;`。DFS 序里 Object 初值铸出 Object 变量(var7), 3 个 String 重赋臂(不相交, 各 `astore S; goto MERGE`)被 `AssignVarGuarded` 按类型串不同**拆出独立 String 变量**, post-merge 的 instanceof/造型读绑到该 String 名 → `(Map)(String)`/`(Collection)(String)` = `String cannot be converted to Map/Collection`。既有 `reachingRefSlot*` 三族都覆盖不到 **Object-current 这条边**: `reachingRefSlotPhiMerge` 只认自-null-check 锚(本形态是 `if(fieldClass==X.class)` 而非 null-check), `reachingRefSlotSubtypeArmMerge` 的 `CrossClassDirectLUB` **刻意永不返回 `java.lang.Object`**, `reachingRefSlotSiblingArmMerge` 的 `CommonSuperType` 算出 Object 后命中其 "strict sibling only" 闸门(`lub==某臂`)而 bail——而那恰好就是某臂为 Object 时。治法: 新增 `reachingRefSlotObjectArmMerge`(shape A 单向)—— 当槽 current 类型**恰为 `java.lang.Object`**、存储值是**非 Object 非基元引用**、且 `slotDefPhiReachesLoad`(下游 load 同时被本 store 与 current 的 def 到达)证 phi 时, **续用 current(Object)**, 具体重赋臂遂成 Object 变量的普通赋值。安全核心: 本修复**绝不改任何变量声明类型**(current 本就是 Object), 只阻止拆分, 故不可能把具体局部加宽成 Object 而破坏其类型特定无造型读——这正是被砍掉的对称 shape B(`current=具体, store=Object` 时 `ResetVarType` 加宽到 Object)实测引发的回归(JSONReaderASCII/ObjectReaderBean/JSONPathParser 等 +多处 `Object cannot be converted to X`/cfs, 净 +1), 故只保留绝对安全的 shape A; phi 闸门是 disjoint 判据(真不相交槽——Object 量死后槽改作无关具体量——无 load 被双 def 共达, 必拆); 对 Object 变量的任何类型特定成员访问在源/字节码里必有显式 checkcast(否则原本编不过), dumper 渲染成 `(T)var`, 故所有读续保类型安全 | `JDEC_REF_SLOT_OBJECT_SUPERTYPE_ARM_MERGE_OFF` | **fastjson2 tree 66→61**(delta -5, A/B 集合 diff: 修复 `ObjectWriterAdapter` 3 行 String→Map/Collection + `JSONPathTwoSegment` 2 行同形态, **新增回归 0 行**; `incompatible types (assignment/return)` 桶 34→29, blockerUnits 28→26), guava 186=186 / gson 0=0 **delta 全 +0(零回归)**。承重 `TestRefSlotObjectArmMergeIsLoadBearing`(真 jar `ObjectWriterAdapter` 单文件即复现, ON=0 / OFF=3 `String cannot be converted to Map/Collection`), 全量 18 个 `IsLoadBearing` 承重测试零回归 + 确定性 `TestZZDeterminism`(fastjson2 681 / gson 195 单元两次反编译逐字节一致)+ 核心反编译单测零回归 |
| **同一 JVM 槽的 store 在嵌套作用域被铸出新 id, 同 VarUid 的 READ 落在兄弟作用域(非该铸造作用域的后代)时漏被重绑、保留 pre-mint 旧 id, 渲染裸 `varN` 与另一处同拼写无关局部撞名(治本, 跨作用域孤儿读「全方法唯一重放」型)** | `incompatible types: JSONPathFunction$SizeFunction cannot be converted to String`(fastjson2 `JSONPathParser.parseFilter` 共 16 行): 该方法 JVM slot 7 被高度复用。先存 `String fieldName`(ref-85)入 slot 7; 后在 `if(fieldName==null)` 臂内, 函数型 segment 的字段名为 null(字节码 offset 1000 `aconst_null; astore 7`), 反编译把该 null 物化成 `Object var12 = null`(ref-94)亦存入 slot 7。各 filter-segment 构造器(`JSONPathFilter$NameIntInSegment` 等)读 slot 7 作 name 实参。`rewriteVar` 在 `if(fieldName==null)` 子作用域里为 null store 铸出新 id `var12`(ref-94), 该子作用域 defer 的 `idReplaceMap` 把**本切片内**的引用重绑到 var12; 但读这些 slot-7 的构造器调用位于**运算符 switch 块(兄弟作用域, 非 `if` 块的后代)**, 其 `ReplaceVar` 够不到——孤儿读保留 pre-mint 旧 id, 渲染成裸 `var11`, 绑到方法后部声明的 `JSONPathFunction$SizeFunction var11`(ref-93, 有声明) → `(String)(var11)` = `(String)(SizeFunction)` → 16 行 `incompatible types`。既有 `reachingSlotVersionGeneral` 修复因见到**多个到达定义**(String 与 Object/null)无法唯一裁决而放弃。根因: `rewriteVar` 每个作用域只重写**自身语句切片**, 跨作用域(兄弟)孤儿读不被任一切片的 `idReplaceMap` 覆盖。治法: `Scope` 新增**全方法共享**的 `allReplace` 累积表, `rewriteVar` 的 defer 把每条 `(oldId→newId)` 重绑写入它; 主 `rewriteVar` 递归结束后(在声明摆放各 pass 之前)新增 `replayUnambiguousRebindings`——对**映射唯一**(某 oldId 在所有作用域都只被重绑到同一个 newId)的条目再做一次**全方法** `ReplaceVar`, 把兄弟作用域的孤儿读补绑到其 store 的声明 id(var12); 不相交槽(一个 oldId 被重绑到多个不同 newId = 真分裂)自动跳过。安全: 只重放**无歧义**重绑(oldId 处处映同一 newId), 真 disjoint 槽分裂(oldId→多个不同 newId)被严格跳过, 故合法的变量分裂零触碰; 在声明摆放前运行使其见到统一 id、把单一声明摆到也支配被补救读的位置; `allReplace` 空时 no-op | `JDEC_ORPHAN_GLOBAL_REBIND_OFF` | **fastjson2 tree 96→66**(delta -30, A/B `OFF-ON=+30`; `incompatible types (assignment/return)` 桶 58→34、`cannot find symbol` 19→13、blockerUnits 31→28; `JSONPathParser.parseFilter` 16 行 `(String)(SizeFunction)` 全清)**+ guava tree 190→186**(delta -4, blockerUnits 127→121, 顺带清同形态跨作用域孤儿读), gson tree 0=0 / iso 44=44 / codec 0=0 **delta 全 +0(零回归)**。承重 `TestOrphanGlobalRebindIsLoadBearing`(真 jar `JSONPathParser` 连同 `JSONPath*` 全家族同编以解析 `$` 兄弟类、令 cast 错显形, ON=0 / OFF=16 `cannot be converted to String`), 全量 17 个 `IsLoadBearing` 承重测试零回归(`TestLiveIntervalSplitIsLoadBearing` 测量期已隔离本开关, 见其注释)+ 确定性 `TestZZDeterminism`(fastjson2 681 / gson 195 单元两次反编译逐字节一致) |
| **synchronized 块本体即 try/catch 时, 合成的正常路径 monitorexit 被下沉进嵌套 try-body, 顶层扫描漏掉致 body 整块丢失(治本, synchronized 结构化「下沉 monitorexit」型)** | `missing return statement`(gson `JsonStreamParser.hasNext:45`): 源码 `public boolean hasNext(){ synchronized(lock){ try { return parser.peek()!=END_DOCUMENT; } catch(MalformedJsonException e){throw new JsonSyntaxException(e);} catch(IOException e){throw new JsonIOException(e);} } }`。javac 把 synchronized 编成 `monitorenter; try{ <body>; monitorexit } catch(any){ monitorexit; throw }`(异常表 `7-27→48 any`)。当 `<body>` 本身又是个 try/catch(内层 `7-25` 捕 Malformed/IOException)时, 结构化把**正常路径的合成 monitorexit(offset 26)+ return(27)** 当作内层 try 的正常后继**吸收进内层 try 的 TryBody 末尾**——于是外层「synchronized 包装 try」的 `TryBody` 只剩 `[内层 TryCatchStatement]` 一个元素, `monitor_exit` MiddleStatement 不在其顶层。`SynchronizeRewriter` 原仅在 `trySt.TryBody` **顶层**线性扫 `monitor_exit`(找到则 `bodySts=TryBody[:i]`、`otherBody=TryBody[i+1:]`), 此形态扫不到 → `bodySts` 留空 → `NewSynchronizedStatement(val, nil)` 发出**空 synchronized 块**, 内层 try/catch + return 整块丢弃 → 方法无 return。治法: 新增 `removeSunkMonitorExit`(DFS 只下潜 `TryCatchStatement.TryBody`)—— 顶层扫不到 monitorexit 时, 递归进嵌套 try-body **就地删除那个被下沉的正常路径 monitorexit**, 并把整个 `trySt.TryBody`(含内层 try/catch/return)作为 synchronized body(此形态无可外提的后继, `otherBody=nil`)。安全: 仅下潜 try 臂(异常路径 monitorexit 在外层 `any` catch 里、本就丢弃, 不动), 故恰好删正常路径那一个; 该回退**只在顶层找不到 monitorexit 时**触发——而该路径原本必然产出空 body(已是 bug), 故只会改善、绝不回归; 普通 synchronized(monitorexit 在顶层)走原有 `foundTop` 分支, 行为逐字节不变 | `JDEC_SYNC_NESTED_MONITOREXIT_OFF` | **gson tree 1→0(清零!)**(`JsonStreamParser.hasNext` synchronized body 完整重建为 `try{return ...;}catch(...){throw...}`, iso 同步 45→44), **fastjson2 iso 305=305 / guava iso 936=936 delta 全 +0(零回归)**(两库无「synchronized 本体即 try/catch」形态, 中性)。承重: gson 确定性 `TestZZDeterminism`(195 单元逐字节一致)+ `test/cross` 全套(含 17 个 `IsLoadBearing`)零回归 |
| **循环归纳变量声明参数化、回边赋值取 raw 字段擦除值, `AssignVarGuarded` 按字符串判不同类型而误拆(治本, raw-vs-参数化同类复用)** | `variable var9_1 might not have been initialized`(gson `LinkedTreeMap.rebalance` 共 5 行 231/251/254/261/269): 源码 `for(Node<K,V> node = unbalanced; node != null; node = node.parent)`——`node`(JVM slot 3)声明类型从形参签名复原为参数化 `Node<K, V>`(entry ref-59), 而回边步进 `node = node.parent` 的 RHS 是**字段读**, 字段 `parent` 的静态类型是**擦除后的 raw `Node`**(value 类型 `LinkedTreeMap$Node`, 无 `<...>`)。`AssignVarGuarded` 用 `ref.Type().String() == typ.String()` 比较, `Node<K, V>` ≠ `Node` 判为**不同类型** → 不复用、`NewVar` 铸出 ref-70(var9_1); 循环体里同一 `aload_3`(node)在 DFS 两次访问被分别绑到 ref-59(var3)/ref-70(var9_1), 部分体读取 var9_1——而 var9_1 仅由体底 277 行 `var9_1 = var3.parent` 赋值, 首迭代前置读 → 未初始化。(注: 一度误判为「循环回边 phi 合并」深根因, slot-version trace 证实纯系 raw/参数化字符串不等导致的误拆, 与回边无关。)治法: `AssignVarGuarded` 在精确串比较失败后, 经 `classifyRawGenericPair` 判「两者**擦除(首个 `<` 前的类名)相同**且**恰一方携带 `<...>` 参数列表**」时复用该槽 ref, 并保留**参数化**类型作声明(槽当前持 raw 侧则 `ResetVarType` 采纳参数化)。安全: 严格限「同擦除 + 恰一方 raw」——两个真不同参数化(`List<String>` vs `List<Integer>`)都带 `<...>`、不满足「恰一方 raw」, 仍按原逻辑拆分; raw 值赋参数化局部是 javac 接受的 unchecked 转换, 故合并恒可编译; 基元/数组/nil 无 `<` 且擦除名互异, 绝不误命中参数化类 | `JDEC_RAW_GENERIC_SLOT_MERGE_OFF` | **gson tree 5→1**(`LinkedTreeMap.rebalance` 5 行 var9_1 全清, `node` 全程统一为参数化 var3; LinkedTreeMap 整类 tree 清零), **fastjson2 iso 305=305 / guava iso 936=936 零回归**(高频 `AssignVarGuarded` 改动, 两大库逐文件失败数完全不变)。承重: gson 确定性 `TestZZDeterminism`(195 单元逐字节一致)+ `test/cross` 全套(含 16 个 `IsLoadBearing`)零回归 |
| **同槽不相交活跃区共享一个 JavaRef、if/else prebind 误拆使另一臂的读被就地重绑覆盖(治本, disjoint 槽族「别名拆分」型)** | `variable var7_1 might not have been initialized`(gson `LinkedTreeMap.find`): 源码 `Node child` 在 `for` 循环里 `child = cmp<0?left:right`(随后 `if(child==null) break; node=child;`), 循环外的插入块用**同一 JVM 槽**(Node 类型)存 `Node created = new Node(...)`。两活区不相交但经循环 break 链接, 栈模拟 `AssignVarGuarded` 见类型相同(Node=Node)**续用同一 `*JavaRef`(ref-28)**。`rewriteVar` 把循环 `child` store 铸成 var7(ID_A), 循环子作用域 defer `ReplaceVar(origId→ID_A)` 就地把**共享的 ref-28.Id** 改成 ID_A, 循环读(共享 ref-28)随之成 var7; 但插入段在兄弟 else 子作用域里, 其内层 `if(root==null)` 的两臂都写 ref-28 且 if 后读(`return created`), `prebindEscapingIfElseSlots` 遂为 ref-28 **再铸一个 ID_B(var7_1)** 并记 `idReplaceMap[ID_A]=ID_B`; else 子作用域 defer 就地把共享 ref-28.Id 从 ID_A 翻成 ID_B, **循环体里的读(共享同一 ref-28 对象)一起被翻成 var7_1**——而 var7_1 在循环路径从未赋值 → `find:82 if(var7_1==null)` 未初始化。关键: `Scope.minted` 是**全方法共享**, ID_A 已记在其中。治法: `prebindEscapingIfElseSlots` 铸新 id 前, 若 `origId`(或 elseRef.Id)**已在 `scope.minted`**(被兄弟/祖先作用域铸过, 即共享 ref 已被就地重绑过)则**直接跳过该 uid**, 让普通 AssignStatement 的 **reuse-minted 路径**(597 行)接管——它复用 ID_A、标 `reused`、由 `placeCrossScopeDeclarations` 把单一 `Node var7;` 提升到公共祖先(与该 pass 整体 OFF 的正确输出逐字一致), 循环与插入合一为 var7。安全: 仅当槽当前 id 已被本方法铸过(确为共享-不相交-别名场景)才跳过, 普通 if/else 逃逸槽(id 未铸)prebind 行为完全不变; 不同类型的同槽复用(如 int vs long[])`AssignVarGuarded` 本就铸独立 ref、`minted` 命不中, 零触碰(fastjson2 SymbolTable 的 `int`/`long[]` 槽不受影响) | `JDEC_IFELSE_PREBIND_MINTED_REUSE_OFF` | **gson tree 6→5**(A/B `OFF-ON`: prebind-reuse OFF=6 / ON=5, `LinkedTreeMap.find:82` 的 `var7_1` 未初始化清零, find 全程统一 var7 可编译)**+ fastjson2 iso 306→305**(delta -1, 顺带修一同形态), guava iso 与 #1/#2 合计 -3, **三库零回归**(gson iso 45 不变; fastjson2/guava iso 单调下降)。承重: 复用既有 `TestLiveIntervalSplitIsLoadBearing`(测量期已隔离本开关, 见其注释), 确定性 `TestZZDeterminism`(gson 195 单元两次反编译逐字节一致)+ `test/cross` 全套零回归 |
| **同槽两不相交臂存子类型引用、`reachingRefSlot*` 漏覆盖子类型臂(治本, disjoint 槽族「子类型合并」型)** | `variable var6 might not have been initialized`(gson `JsonAdapterAnnotationTypeAdapterFactory:46`): 同一 JVM 槽在不相交臂分别存 `TypeAdapter`(基)与其 **jar 内子类** `TreeTypeAdapter`, 汇合处当一个 `TypeAdapter` 用。`reachingRefSlotPhiMerge` 只处理「自 null-check 锚定」的子类型, `reachingRefSlotSiblingArmMerge` **显式跳过子类型关系**(只处理兄弟), 二者都够不到「子类型臂 + 无 null-init 锚」这一形态 → `TreeTypeAdapter` 被铸成独立 var8, 基类 var6 在该路径未赋值。治法: 新增 `reachingRefSlotSubtypeArmMerge`—— 当存储值类型是槽 current 类型的**严格 jar 内子类型**(经 `ClassContext.SiblingSuperTypes` + `types.IsSubtypeVia`/`CrossClassDirectLUB`)且 phi 证为同一变量时, **续用 current(基类)ref**(子类值可直接赋基类型), 两臂遂成一个 `TypeAdapter` 变量。安全: 严格限「jar 内严格子类型 + phi 同变量」; 无子类型关系/仅共 Object 返回 nil(那是真不相交变量); 无 resolver(单类反编译)provider 空, no-op | `JDEC_REF_SLOT_SUBTYPE_ARM_MERGE_OFF` | **gson**: 修复后 `JsonAdapterAnnotationTypeAdapterFactory` 正确输出 `var6 = new TreeTypeAdapter(...)`(该文件 definite-assignment 清零, 解遮蔽出下游 LinkedTreeMap), fastjson2/guava iso 与 #1/#3 合计零回归。承重: 解遮蔽链(JsonWriter→JsonAdapter→LinkedTreeMap)+ 三库 iso A/B 单调不增 |
| **for 循环里 `continue` 跳向自增 latch(而非循环头)被结构化丢弃(治本, 循环结构化「分裂-continue」型)** | `variable var7 might not have been initialized`(gson `JsonWriter.string`/`stream/JsonWriter`): `for` 循环体有**两个及以上**早退 `continue` 跳向体底唯一自增节点 `i++`(`if(c<128){r=arr[c]; if(r==null) continue;} else if(...) ... else continue; <write r>; i++;`)。该自增节点是 if/else 区域的**后支配点**, 这些 `goto i++` 边**不是回到循环头的回边**, 故 `RebuildLoopNode` 不重定向、`LoopJmpRewriter` 的 `next==circleNode` continue 规则不触发, 边作为普通 fall-through 落入自增; `IfRewriter` 遂把分支结构化成**空体**(continue 体只是被丢的 goto), if 后的 `out.write(r)` 在 `r` 从未赋值的路径上无条件执行 → 未初始化。治法: 新增 `convertSplitContinueToLatch`(在 `LoopJmpRewriter` 开头): 对自增 latch(`asLatchIncExpr` 识别裸 `i±1`、唯一后继是循环头、被循环头支配)的每个**条件/跳转前驱**, splice 一份**私有自增副本 + 显式 `continue`**(`P → i++' → continue → 循环头`), 使每条 continue 路径先步进再重测, latch 自身的 fall-through 路径不动。闸门: 单一 ±1 latch、唯一后继为循环头、**≥2 个条件/跳转前驱 且 ≥1 个 fall-through 前驱**——单 continue 循环(IfRewriter 分支反转已处理)与无可分离步进 latch 的循环逐字节不变 | `JDEC_SPLIT_CONTINUE_LATCH_OFF` | **gson**: `JsonWriter.string` 两个 `continue` 恢复(`var5++; continue;`), 解遮蔽出 JsonAdapter/LinkedTreeMap; **顺带上游治本 fastjson2 `ObjectWriterAdapter`** 的断裂 continue → 其孤儿局部获正确声明(故 `TestNullInitNarrowDecl` 测量期已隔离本开关, 见其注释), fastjson2/guava iso 与 #2/#3 合计零回归 | 
| **`instanceof` 操作数被 `OP_INSTANCEOF` 的 CustomValue 冻结身份, 漏传 ReplaceVar(治本, disjoint 槽渲染期撞名)** | `unexpected type / required: reference / found: int`(fastjson2 `JSONSchema.of` 共 9 行): 源码 `Object exclusiveMaximum = map.get(...); if (exclusiveMaximum instanceof Integer) ...`。同一 JVM 槽(slot 6)在 `if/else` 不相交臂里先后承载 `int`(数组长度等)与 `Object`(`map.get` 结果), `rewriteVar` 正确地为二者各 mint 一个 `*VariableId`(int 版本 `var6` 与 Object 版本 `var6_3`), store 端的引用被 `ReplaceVar(oldId→var6_3)` 重绑成 Object。但 `instanceof` 操作数是在栈模拟期被 `OP_INSTANCEOF` 分支**捕获进一个 `CustomValue` 的 `String` 闭包**的——而该 `CustomValue` 创建时**只传了 `stringFunc`/`typeFunc`、漏传 `replaceFunc`**(不同于 `OP_CHECKCAST` 与数值转换的 CustomValue, 它们都转发了 ReplaceVar)。于是末期 `rewriteVar` 的 `ReplaceVar(oldId→var6_3)` 到达不了被冻结在闭包里的操作数, 操作数仍渲染成原始的 int 身份 `var6`, 而那个名字此刻已绑给**另一个不相交的 int 局部** → `var6 instanceof Integer`(`var6` 是 int)→ javac `unexpected type / required: reference / found: int`。治法: `code_analyser.go` 的 `OP_INSTANCEOF` 分支给 `NewCustomValue` 补上 `replaceFunc` 闭包, 像 `OP_CHECKCAST` 那样把 `ReplaceVar(oldId, newId)` 转发给捕获的操作数 `value`, 使操作数身份随末期重绑同步更新。安全: 仅多转发一次 ReplaceVar 给本就该跟随重绑的操作数(与 checkcast/数值转换完全同构), 不改变任何类型/渲染逻辑; 操作数无身份变化时转发为 no-op; 正常(非 disjoint 复用)`instanceof` 的操作数 id 末期不被重绑, 转发亦为 no-op → 零触碰 | `JDEC_INSTANCEOF_REPLACEVAR_OFF` | **fastjson2 tree 130→121**(delta -9, A/B `OFF-ON=+9`; `unexpected type` 桶 9→0, `JSONSchema.of` 的 `var6/var7 instanceof Integer`、`var4 instanceof String` 等全部正名), guava 193 / gson 1 / codec 0 **delta 全 +0(零回归, 任一 jar 无新增破坏类)**。承重 `TestInstanceofReplaceVarIsLoadBearing`(真 jar `JSONSchema`, ON=0 / OFF=9 `unexpected type`), 全量 classparser + decompiler core + 确定性 + 结构往返/承重测试零回归 |
| **兄弟臂引用 phi 合并: 同槽两不相交臂存兄弟引用类型按 LUB 定型(治本, disjoint 槽族「合并到 LUB」型)** | `inconvertible types: Field cannot be converted to Method` / `Class cannot be converted to Field` 等(fastjson2 `FieldWriter.compareTo` 一处就 16 行, 另一类同形态共 -17): 源码 `Member m1; if (cond) m1 = this.method; else m1 = this.field; ...; if (m1 instanceof Method) ((Method) m1).getReturnType()`。同一 JVM 槽在 `if/else` **两不相交臂**分别存 `Method`(`this.method`)与 `Field`(`this.field`)——二者都 `<: java.lang.reflect.Member` 但**互无子类型关系**(兄弟)——汇合处多态使用(`instanceof Field/Method`、`(Method)`/`(Field)` 造型、`getDeclaringClass`)。字节码序下 if 臂先 store(DFS 铸出 `Method` ref R1), else 臂后 store(`current=R1`、`val=Field`); `AssignVarGuarded` 见类型串不同且非 null-init/形参/int 类, **按类型拆**出独立 `Field` 名(`var6`)与 `Method` 名(`var6_4`), 汇合 use 绑到 `Field` 名 → `var6 instanceof Method`/`((Method)var6)` 因「Field 永不可能是 Method」全部 `inconvertible types`。这是 §3.5 disjoint 槽族的**「合并到 LUB」型**——区别于已治本的 boolean 兄弟臂(`reachingBoolSiblingArmMerge`, 同类型 boolean)与子类型臂(`reachingRefSlotPhiMerge`, `List ← new ArrayList`), 本族是**兄弟引用类型**, 真实类型是两臂 LUB。治法: 新增 `reachingRefSlotSiblingArmMerge`(`reachingRefSlotPhiMerge` 的兄弟版): 当槽 current 与存储值都是**非空非形参引用**、类型串不同、`types.CommonSuperType` 给出**非 Object 的 JDK 表公共上界 L**(且 L 严格是双方上界、非任一臂自身)、且 `slotDefPhiReachesLoad`(下游 load 同时被本 store 与 current 的 def 到达)证 phi 时, 把 current **加宽到 L**(`Method/Field → Member`)并续用(`refPhiMerged` 旁路 `AssignVarGuarded`), 两臂遂成一个 `Member` 变量的普通赋值。安全: `CommonSuperType` 对**只共 Object** 的 Map/List/Number/String 派发型槽复用返回 nil(那是真不相交的不同变量)、对非 JDK 表类型也返回 nil, 故只命中 JDK 兄弟家族(反射/Number/CharSequence/集合); phi 闸门保证同一源变量(一处源码读只能观测一个变量); 原字节码既已编译, 每臂值必可赋给 L, 且类型特定 use 本就是 dumper 从 checkcast 渲染的显式 `((Method) m1)` 造型, 故加宽到 L 恒类型安全 | `JDEC_REF_SLOT_SIBLING_ARM_MERGE_OFF` | **fastjson2 tree 115→98**(delta -17, A/B `OFF-ON=+17`; `FieldWriter.compareTo` 16 行 `Field/Class/Method` 互转全清 + 另一处同形态 1 行, `FieldWriter` 单元整类清零), guava 190 / gson 1 / codec 0 **delta 全 +0(no-op, 零回归)**。承重 `TestRefSlotSiblingArmMergeIsLoadBearing`(真 jar `FieldWriter`, ON=0 / OFF=16 `cannot be converted to Method/Field`), 全量 15 个 `IsLoadBearing` 承重测试零回归 + fastjson2 整 jar 681 文件两次反编译逐字节一致(确定性) |
| **跨类(jar 内)三元 LUB 加宽: 直接子类型关系驱动声明上行(治本, 仅 ref 不动三元缓存)** | `bad type in conditional expression`(fastjson2 schema 包 `AllOf:56`/`ArraySchema:133`/`ObjectSchema:136,170` 等 6 行 + guava 同形态 3 行): 源码形如 `Any var6 = null; ...; var6 = cond ? Any.INSTANCE : Any.NOT_ANY;`, 两臂都是 **jar 内**类 `Any` 的静态字段(`Any extends JSONSchema`)。旧 `ternaryDeclLUB` 两处够不到: (1) 它只查 **JDK 静态层级表**(`hierarchy.go`), 不识别 jar 内 `Any <: JSONSchema`, `MergeTypes` 退回首臂; (2) 它**仅在 `isFirst`(声明即初始化)** 时触发, 而这里是「先 `= null` 声明、后单独 `var6 = 三元` 赋值」形态, 声明 store 不带三元、三元 store 非 isFirst, 故永不触发。于是 `var6` 停在 `Any`(或更早的 `UnresolvedReference`), 而 javac 按 `lub(Any.INSTANCE, Any.NOT_ANY)` 推断出 `Any` 之上的上界, 二者不可互转 → `bad type in conditional expression`。治法: 新增 `ternaryDeclLUBCrossClass`(`ternaryDeclLUB` 的跨类伴生), 借新增的 `ClassContext.SiblingSuperTypes`(经 `dumper.buildSiblingSuperTypes` 用 `foldSiblingResolver` 加载 jar 内类、取其 RAW 直接 `super_class`+`interfaces` 内部名)与新原语 `types.CrossClassDirectLUB`/`IsSubtypeVia`(沿 RAW 父类型链判直接传递子类型), 当三元两臂存在直接子类型关系时把声明 ref 加宽到上界臂; 在 `astore` 点调用且**不**受 `isFirst` 闸门约束(以覆盖 null-init 后赋值形态)。关键安全设计: **只 `ResetVarType(ref)`, 绝不调 `tv.SetCachedType`**(与 JDK `ternaryDeclLUB` 相反)—— 跨类目标都是**独立声明**局部(声明类型从 ref 渲染, 缓存多余); 且 `SetCachedType` 会改写**共享** ternary 的缓存类型, 当某 `?:` 的臂是参数/`this` ref(实参物化的 `(var2 == null) ? this : var2` 传给 `JSONSchema.of`)时会**回流改窄**该参数声明, 把 `ObjectSchema(JSONObject, JSONSchema)` 形参错窄成 `ObjectSchema`(实测 +7 新错, 净 +1 回归)——故 ref-only 把加宽限制在「被声明的那个变量」本身。另两道闸门: **WIDEN-ONLY**(`IsSubtypeVia(rt, lub)` 证当前 ref 类型确是 LUB 子类型才动, 防陈旧/意外 ref 跨到兄弟类)+ provider 非空(仅 `DecompileWithResolver`/jar 路径生效, 单类反编译为 no-op) | `JDEC_TERNARY_DECL_LUB_CROSS_OFF` | **fastjson2 tree 121→115**(delta -6, A/B `OFF-ON=+6`; `bad type in conditional expression` 桶清 AllOf/ArraySchema/ObjectSchema 等 6 行, ObjectSchema 的 `JSONSchema cannot be converted to ObjectSchema` 回归已随 ref-only 设计消除)**+ guava tree 193→190**(delta -3, 顺带清同形态), gson 1→1 / codec 0→0 **delta 全 +0(no-op, 零回归)**。承重 `TestTernaryDeclLUBCrossIsLoadBearing`(真 jar AllOf/ArraySchema/ObjectSchema, ON=0 / OFF=4 `bad type in conditional`), 全量 14 个 `IsLoadBearing` 承重测试零回归 + fastjson2 整 jar 681 文件两次反编译逐字节一致(`TestZZDeterminism` 确定性) |
| **孤儿生成局部被 dumper 文本兜底默认 `Object varN = null`: 用 AST 解析类型合成正确声明(治本, undeclared-orphan 误型)** | `cannot find symbol: method write, location: variable var30 of type Object`(fastjson2 `ObjectWriterAdapter.writeWithFilter` 共 3 行): 源码 `ObjectWriter w = fieldWriter.getObjectWriter(...); w.write(...)`。`var30` 在 RewriteVar 末期**既无任何声明**(跨作用域 hoist 把它原本的 `IsFirst` 声明降级却没补回裸声明), 仅有三处 `var30 = var19.getObjectWriter(...)`(赋值)与 `var30.write(...)`(读)。dumper 的文本兜底网 `addMissingGeneratedLocalDecls` 见 `var30` 未声明便合成声明, 但 `inferGeneratedLocalRefType` **只识内嵌赋值** `(varN = X)`、不识普通语句 `varN = recv.m(...)`, 且 `recv=var19` 是 `FieldWriter`、`getObjectWriter` 是**跨类**方法, 文本无符号表无从解析返回类型 → 只能默认 `Object var30 = null`, 三处 `var30.write(...)` 全部 `cannot find symbol: method write, location: variable var30 of type Object`。关键: AST 里该 reassignment 的 RHS 值类型在栈模拟期**已解析为 `ObjectWriter`**(`FunctionCallExpression/ObjectWriter`), 比文本兜底强得多。治法: `RewriteVar` 末新增 `narrowNullInitObjectDecl`—— 按名字收集每个生成局部的「(去重)非 null reassignment RHS 类型集 S」「是否有 `Object varN`/`= null` 声明」「是否有任何声明」; 当 S 恰为**单一非 Object 非基元引用类型**且 `nameUsedOnlyAsReceiverOrLHS`(该名所有文本出现皆为 `varN.x` 接收者或 `varN =` 赋值目标)为真时: (a) 若已有 Object 声明→`ResetVarType(S)` 收窄(连同绑到它的读); (b) 若**完全无声明**(孤儿)→把 reassignment ref 重定型为 S, 在方法首部 prepend `S varN;`, 使 dumper 见已声明而跳过 Object 兜底。安全: 声明类型只管成员解析与赋值兼容, 每个存储都是 S(或 null, 可赋任意引用), 故声明仍接受全部赋值; 接收者/赋值-目标-only 闸门保证 `varN` 不作裸实参传递, 收窄静态类型**不改变任何重载选择**(唯一变化是 `varN.member` 改在 S 上解析, 正是字节码 invoke 的目标成员); 仅当 reassignment 类型唯一时触发, 真多类型/真 Object 槽零触碰; 方法首裸 `S varN;` 满足明确赋值(JVM 校验器已保证存储支配每个读, 同 `placeCrossScopeDeclarations` 依赖的不变式) | `JDEC_NULLINIT_NARROW_OFF` | **fastjson2 tree 133→130**(delta -3, A/B `OFF-ON=+3`; 清 `ObjectWriterAdapter.writeWithFilter` 三处 `var30.write` 的 `of type Object` 接收者错), guava 193 / gson 1 / codec 0 **delta 全 +0(零回归, 任一 jar 无新增破坏类)**。承重 `TestNullInitNarrowDeclIsLoadBearing`(真 jar `ObjectWriterAdapter`, ON=0 / OFF=3 `of type Object` 接收者错), 全量 classparser + decompiler core + 确定性 + 结构往返/承重 + `test/cross` 全套零回归 |
| **同槽拆出两个同名 `varN` 之一无声明: 名字制作用域覆盖安全网(治本, split-id undeclared 残余)** | `cannot find symbol: variable varN`(fastjson2 三处共 10 行): (a) `ObjectWriterAdapter.writeWithFilter` 的 fieldWriter 循环 `var19`(FieldWriter)/`var21`(String) —— 在外层(循环体 / 嵌套 else 臂)被直接赋值/读取, 但**唯一**的 `T varN` 声明被 `placeCrossScopeDeclarations` 沉到更深的嵌套 else 臂里; (b) `JSONReaderUTF16`/`JSONReaderUTF8.readOffsetDateTime` 的 `char var7` —— 在 `if` **条件**里内嵌赋值 `(var7 = a[i]) != ' '` 处使用, 声明却在该 if 的 else 臂里。根因: `rewriteVar` 把同一 JVM 槽(`varN` 即槽深派生名)**拆成两个 `*VariableId`**, 二者渲染成相同 `varN`, 其中一个**只被赋值/读取、没有自己的声明**; 而 `placeCrossScopeDeclarations` 是**按 id** 收集候选(`collectGeneratedLocalDeclIDs` 只收有声明语句的 id), 那个无声明的 id 对它**完全不可见**, 其 `topLevelDeclDominatesAllUses`(按 id 探针改名)只验**有声明那个 id 自己**的引用是否在域内(恒 true), 故永不提升 → 无声明 id 的使用 `cannot find symbol`。因两个 id 共享槽派生名、**javac 按名字解析局部变量**, 治本是把「已存在的同名声明」扩到能**词法覆盖该名字所有文本出现处**的最低公共祖先块。治法: `RewriteVar` 末新增**按名字**的安全网 `coverUndeclaredGeneratedLocals`(在 `placeCrossScopeDeclarations`+`dropDuplicateDeclarations` 之后跑): 对每个有声明的 `varN`(`genLocalDeclByName` 收集名→代表 ref + **去重类型集**), 当 (1) 类型唯一(多类型=真不相交槽, 不碰)、(2) 本块顶层无该名声明、(3) `blockHasUncoveredRefByName` 证实子树里有**未被任何同名声明词法覆盖**的出现(含 if/loop **条件 head** 里的内嵌赋值/读取, 由 `statementHeadReferencesName` 经「整体渲染 n 计数 − 各子体 n 计数 = head 计数」识别)、(4) 本块是该名所有出现的 LCA(`cnt≥2` 或单容器但 head-ref / 跨 ≥2 子作用域)四条全真时, `relocateDeclarationsByName` 把子树里该名的就地声明降级/裸声明删除, 再在本块顶 prepend 单个 `T varN;`。安全: **只在某名字确有越界出现(本就编不过)时才动**, 正确作用域的代码 `blockHasUncoveredRefByName` 返 false 零触碰; 提升只**扩大**作用域, 恒为合法 Java; 单一类型闸门排除真不相交同名槽(避免误并不同类型); head 识别只多覆盖、不误报(渲染失败保守返 false) | `JDEC_COVER_UNDECLARED_OFF` | **fastjson2 tree 143→133**(delta -10, A/B `OFF-ON=+10`; 清 `ObjectWriterAdapter`(var19×4/var21×2)+ `JSONReaderUTF16`/`JSONReaderUTF8`(var7×2 各)全部 `variable varN` undeclared, `ObjectWriterAdapter` 单元整类清零), guava 193 / gson 1 / codec 0 **delta 全 +0(零回归, 任一 jar 无新增破坏类)**。承重 `TestCoverUndeclaredLocalIsLoadBearing`(真 jar 三类, ON=0 / OFF=10 `variable varN`), 全量 classparser + decompiler core + 确定性 + 结构往返/承重测试零回归 |
| **内联 lambda 体懒解析途中污染外围方法 FuncCtx(治本, 跨方法上下文复用根因)** | `int cannot be converted to boolean`(fastjson2 `BeanUtils.isWriteEnumAsJavaBean(Class):boolean` 的 `return 1;`/`return 0;` 各 1 行): 该 boolean 方法体内含一个 invokedynamic lambda(`(Method l0) -> processJSONType1x(...)`, 其 impl 合成方法 `lambda$...$2` 返回 **void**), 且方法**尾部**有 `return false;`(javac: `iconst_0; ireturn`, 偏移在 invokedynamic 之后)。根因在**架构级上下文复用**: lambda arrow 体是在外围方法做栈模拟、构造 invokedynamic 值时**就地懒解析**的(`bootstrap_methods.go` 的 metafactory 闭包 → `d.DumpClassLambdaMethod` → 递归 `dumper.DumpMethodWithInitialId`); 而 `parser.FunctionContext` **就是** `dumper.FuncCtx` 同一指针, 递归 dump 把它的 `FunctionName`/`FunctionType`(及 `MethodType`/`CurrentMethod`/`IsStatic`)覆写成 lambda 的(`lambda$...$2` / void)且**解析完不还原**。于是外围方法位于 invokedynamic **之后**的剩余字节码(尾部 `return false/true`)在 `resetReturnValueTypeSafe` 里拿到的是 lambda 的 **void** 返回类型, `int→boolean` 重定型(`JavaTypeWrap.ResetType` 仅 `int→boolean`/`void→T` 才改)**不触发**, 字面量 1/0 原样渲染成 `return 1;`/`return 0;` → javac `int cannot be converted to boolean`。治法: 在 `classparser/code_analyser.go` 的 `DumpClassLambdaMethod` 闭包里, 递归 lambda dump **前保存、后还原**外围方法的 per-method 身份(`FuncCtx.FunctionName/FunctionType/IsStatic/TypeParams` + `dumper.MethodType/CurrentMethod`), 使外围解析在 lambda dump 返回后立即恢复自身上下文。安全: 只还原**每方法身份**这几个标量(不碰 `dumpedMethodsSet` 缓存与累积的 import — 那些本就该跨方法持久), 还原点正好在控制权回到外围 `ParseBytesCode` 之前; 正常(非 lambda)方法 dump 路径**完全不经过此闭包**, 零影响; 嵌套 lambda 各自的闭包按 LIFO 正确组合。这是个跨方法上下文泄漏的**通用治本**(不止 boolean return — 任何「invokedynamic 之后还依赖外围 FunctionType 的逻辑」都受益) | `JDEC_LAMBDA_CTX_RESTORE_OFF` | **fastjson2 tree 145→143**(delta -2, A/B `OFF-ON=+2`; 清 `BeanUtils.isWriteEnumAsJavaBean` 的 `return 1`/`return 0` 两行 int→boolean), guava 193 / gson 1 / codec 0 **delta 全 +0(零回归, 任一 jar 无新增破坏类)**。承重 `TestLambdaCtxRestoreIsLoadBearing`(真 jar `BeanUtils`, ON=0 / OFF=2 int→boolean 错), 全量 classparser + decompiler core + 确定性 + 结构往返/承重测试零回归 |
| **布尔方法 `return false/true` 复用不相交 int 槽: 拆分而非合并(治本, disjoint 槽族首个「拆分」型)** | `int cannot be converted to boolean`(922/925)+ `boolean cannot be converted to int`(927): guava `LocalCache$Segment.replace(K,int,V,V)` / `MapMakerInternalMap$Segment.replace` 各 3 行。源码 `int newCount = this.count - 1; ...; this.count = newCount;`(在 `if (valueRef.isActive())` 内, JVM slot 14 作 **int 活区**), 随后无条件 `iconst_0; istore 14`(= `boolean result = false`)**杀死**该槽并 `return result`(**boolean 活区**)。两活区互不相交(int store 的值在本 store 前被 putfield 消费, 本 store 的值只被其后的 boolean IRETURN 消费), 但因 `return false` 在 JVM 栈上是 int-0/1 字面量, `AssignVarGuarded` 视其与槽内 int ref 同类而**续用**, 把两个不相交活区合并成一个变量; 随后 `IRETURN` 经 `resetReturnValueTypeSafe` 把该共享 ref 重定型为 boolean, 于是先前的 int 用法变成 `boolean = count-1`(922/925)与 `this.count = <boolean>`(927)。这是 §3.5 disjoint 槽族的**「拆分」方向**(此前所有 `reachingBool*Merge` 都是「合并」方向: 把 int-0/1 续到 boolean def; 本族相反)。两活区为不同到达定义 web(int store 的值被 putfield 消费在前、本 store 的值被 IRETURN 消费在后), 由 `computeSlotWebs` 可证。治法: 新增 `reachingBoolReturnSlotSplit`—— 当槽 current 版本是**纯 int 局部**(`!IsParam` 且恰 int, 即 AssignVarGuarded 本会续用)、存储值是 int-0/1 字面量、`functionReturnsBoolean` 为真、`slotStoreFeedsReturn`(前向到达一个被 ireturn 直接返回的同槽 load)且 `slotStoreDisjointFromCurrentWeb`(current 的 def 不在本 store 的 web = 真不相交复用)四条全真时, 把 0/1 **重定型为 boolean**, 于是 `AssignVarGuarded` 见 boolean(非 int 范畴)而**新铸一个 boolean 变量**, 把 return 区间从 int 区间拆开。安全: 四条合取极窄, 精确锚定「布尔方法 return false/true 复用了一个先前的 int 局部槽」—— 此处拆分恒正确(布尔结果与先前的 int 是两个源变量); 与 `reachingBool*Merge` 族在 current 类型上互斥(本族要求 int, 它们要求 boolean), 永不冲突; web 不可用(`JDEC_LIVEINTERVAL_OFF`)时 `slotStoreDisjointFromCurrentWeb` 返 false 自然不触发 | `JDEC_BOOL_RETURN_SLOT_SPLIT_OFF` | **guava tree 199→193**(delta -6, A/B `OFF-ON=+6`; 清 `LocalCache$Segment` + `MapMakerInternalMap$Segment` 各 3 行 int↔boolean, 其中一个 Segment 单元整类清零, blockerUnits -1), fastjson2 145 / gson 1 / codec 0 **delta 全 +0(零回归, 任一 jar 无新增破坏类)**。承重 `TestBoolReturnSlotSplitIsLoadBearing`(真 jar `LocalCache$Segment`, ON=0 / OFF=3 int↔boolean 错), 全量 classparser + decompiler core + 确定性 + 结构往返/承重测试零回归 |
| **布尔形参在分支被 int 0/1 重赋后于汇合处读取: 续用形参不分裂(治本, disjoint 槽族首攻)** | `bad operand types for binary operator '!='/'=='`(fastjson2 `ObjectWriterProvider.getObjectWriterInternal` 一处就 7 行, 全族共波及多类): 源码 `boolean fieldBased`(形参)在 `if` 内被 `fieldBased = false;` 重新赋值, 随后 `fieldBased ? cacheFieldBased.get : cache.get`(及多处 `if(fieldBased)`)读取。javac 把 `fieldBased = false` 编成 `iconst_0; istore S`(**int 范畴**, JVM 操作数栈无 boolean), 而该槽的到达定义是**方法入口形参**(live-in)而非 store 指令; 既有 `reachingBoolSiblingArmMerge` 的 phi 判据 `slotDefPhiReachesLoad → reachingSlotStoreRefs` **只认 store 定义、不认入口形参**, 故无法锚定形参臂 → int-0/1 store 被 `AssignVarGuarded`(int↔boolean 不可转)分裂出独立 int 量。汇合读以**槽深派生的 `varN` 命名**(并非槽号), 与同深度的无关引用局部(此处 `String var6` 即 `var2.getName()` 拷贝)撞名, 且该 int 量**在作用域内无声明**, javac 把读绑到 `String var6` 声明 → `String != int`。这是 disjoint 槽/变量复原族(§3.5)在「布尔形参重赋」子形态上的精确根因(非 web 引擎能修: `JDEC_LIVEINTERVAL_WEB` 全开 load 侧因模拟序对此 0 命中、store 侧 +2 净负)。治法: 新增 `reachingBoolParamReassignMerge`(`reachingBoolSiblingArmMerge` 的形参镜像)—— 当槽 current 版本是 **boolean 形参**(`IsParam`)、存储值是 int-0/1 字面量、且 `slotStoreReachesParamPhiLoad` 证实「本 store 前向可达的某 slot load 同时经 `reachingStoresOf` 无-store 路径到达方法入口」(形参臂与 store 臂汇入同一读 = phi)时, 把 0/1 重定型为 false/true 并**续用形参 ref**(不分裂)。安全: 严格限「boolean 形参 × int-0/1 × 形参-store 双臂 phi」; 真 disjoint 槽复用(形参死后槽改作新 int)每个读恒被自身 store 支配, 无 load 能无-store 到达入口, 故 `slotStoreReachesParamPhiLoad` 必返 false, 自然不触发——单调安全 | `JDEC_BOOL_PARAM_REASSIGN_MERGE_OFF` | **fastjson2 tree 161→145**(delta -16, A/B `OFF-ON=+16`; `bad operand types` 桶清 13 `!=`+5 `==` 中本族部分, `ObjectWriterProvider` 单元整类清零, 另波及数类各减若干), guava 199 / gson 1 / codec 0 **delta 全 +0(零回归, 无任何类新增破坏)**。承重 `TestBoolParamReassignMergeIsLoadBearing`(真 jar `ObjectWriterProvider`, ON=0 / OFF=7 bad-operand 错), 全量 classparser 单测 + decompiler core + 确定性 + 结构往返/承重测试零回归 |
| **未被使用的 lambda 形参隐式渲染(去显式类型)(治本)** | `incompatible parameter types in lambda expression`(fastjson2 ObjectReaderCreatorASM 的 `rawMap.computeIfAbsent(Integer.valueOf(k), (Integer l0) -> { return new ArrayList(); })` 共 4 处): 接收者是**裸类型** `Map`(局部 `new TreeMap()`, 泛型未复原), 其 `computeIfAbsent` 的 SAM 退化为 **raw `Function`**(`apply(Object)`); 而 lambda 形参从 impl-method 描述符复原出显式类型 `(Integer l0)`, javac 见 `Integer ≠ Object` 报 `incompatible parameter types in lambda expression`。关键观察: 这些 lambda 体**根本没用到形参**(`return new ArrayList()`), 故显式类型可整段去掉。治法: `DumpMethodWithInitialId` 在拼接 lambda arrow 前, 经 `lambdaParamsUsed`(检测体内是否出现任一 `lN` 形参名, 词边界匹配; 形参名 `lN` 专属 lambda 形参、体内自有局部已重命名为 `lv<seq>_N`, 故无歧义)判定**全部形参皆未使用**时, 把 `paramsNewStr` 改为**仅形参名**(隐式 `(l0) -> ...`), 让 javac 从目标 functional interface 推断类型。安全: 严格限「**全部形参皆未使用**」—— 体内引用某形参为具体类型的 lambda 保留显式声明(行为不变, 也回避隐式形参在重载消歧上的理论风险); 隐式 lambda 的目标类型恒由赋值/调用/返回上下文提供, 故隐式形参恒可推断; 这也更贴近源码惯用写法(`k -> new ArrayList()`) | `JDEC_LAMBDA_IMPLICIT_UNUSED_PARAM_OFF` | **fastjson2 tree 165→161**(delta -4, A/B `OFF-ON=+4`; `incompatible parameter types in lambda` 桶清掉 4 处全未用形参的 computeIfAbsent), guava 199 / gson 1 / codec 0 **delta 全 +0(零回归)**。承重 `TestLambdaImplicitUnusedParamIsLoadBearing`(真 jar `ObjectReaderCreatorASM`, ON=0 / OFF=4 lambda-param 错), 全量 classparser + 结构往返/承重测试零回归, fastjson2 两次反编译产物逐字节一致(确定性) |
| **switch 合并点的非支配前驱误改写成 break(治本)** | `break outside switch or loop`(fastjson2 ObjectReaderImplMap.of / JSONReader 家族, 共 31 处的主因): 一个 `switch` 之前有 `if/else-if` 链, 链中各分支前向跳到与该 switch **相同的合并点**(post-switch continuation, 如 `var = X.class; goto merge`), 而 switch 的 `case` 也 break 到同一 merge。`SwitchRewriter1` 给 merge 节点的**所有**前驱(`mergeNode.Source`)都插入 `break` 叶子, 把这些**位于 switch 之外**的 if 链边也变成裸 `break` → javac 报 `break outside switch or loop`(if/else-if 已天然汇流到 merge, 这些 break 既冗余又非法)。治法: 插 break 前加支配性闸门——仅当前驱被 switch 节点**传递支配**(`utils.IsDominate(DominatorMap, node, source)`, 确属 switch 区域/case 体)才插 break; switch 之外的前驱(合并点的其它入边)**保留原边**, 让控制流自然汇入 merge(下游 if 结构化按 fall-through 渲染)。安全: case 体节点恒被 switch 传递支配(switch 是 cases 唯一入口), 必保留 break; switch 之外的节点不可能 break 出一个它不在其中的 switch, 故丢弃的恒为非法 break——单调安全 | `JDEC_SWITCH_NONDOM_MERGE_BREAK_OFF` | **fastjson2 tree 189→165**(delta -24, A/B `OFF-ON=+24`; `break outside switch or loop` 桶 31→0, 同方法内被该 parse 错掩盖的少量语义错随之显形为 `bad operand types`, 净 -24)**+ guava 200→199**(顺带清一处同形态), gson 1 / codec 0 **delta 全 +0(零回归)**。承重 `TestSwitchNonDomMergeBreakIsLoadBearing`(真 jar `ObjectReaderImplMap`, ON=0 / OFF=5 break-outside-loop 错), 全量 classparser + 结构往返/承重测试零回归 |
| **反射家族三元 LUB: 层级表反射行 + 三元声明缓存刷新(治本, 两耦合)** | `Member m = cond ? this.method : this.field`(fastjson2 FieldReader.toString/compareTo + FieldReaderObject): 合并 `java.lang.reflect.Method` 与 `Field`。两个耦合根因——(1) 层级表(`hierarchy.go`)缺反射家族, `MergeTypes(Method, Field)` 退回首臂 `Method`; (2) 即便加表, `ternaryDeclLUB` 只重置左值 ref, 而声明类型渲染取自 RHS 三元的 `cachedType`——该缓存是**装配期**(臂未定型前)铸出的窄类型 `Method`, 故声明仍印 `Method var1`。二者任一不修都得 `Method var1 = cond ? method : field` → javac `bad type in conditional expression`(Method 与 Field 不可互转)。治法: (1) 表新增 `Method/Field/Constructor → Member`(**刻意只映射接口 Member, 省略类 AccessibleObject**: 合并值上实际调用的是 getName/getDeclaringClass/getModifiers, 全在 Member 上、AccessibleObject 一个都没有; 选 AccessibleObject 只会把 `bad type in conditional` 换成 `cannot find symbol: getName()`; javac 真实算出的交集类型 `AccessibleObject & Member` 在声明里不可命名, 故 Member 是唯一 use-correct 选择); (2) `ternaryDeclLUB` 加宽时同步 `tv.SetCachedType(vt)` 刷新三元缓存, 使 `value.Type()` 与加宽后的 ref 一致。安全: 表为保守小表, 反射行只影响 Method/Field/Constructor 互并(极罕见), A/B 全 jar 零回归; 缓存刷新只在 ternaryDeclLUB 已决定加宽(已重置 ref)时同步, 严格纠正缓存陈旧, 无副作用 | `JDEC_TYPELUB_OFF`(反射行/整表) / `JDEC_TERNARY_DECL_LUB_CACHE_OFF`(缓存刷新) | **fastjson2 tree 195→189**(delta -6; `incompatible types` 桶 85→79, 清 FieldReader 165/214/215 三元声明 + FieldReaderObject:192 重赋 + 连带), guava 200 / gson 1 / codec 0 **delta 全 +0(零回归)**。承重 `TestReflectMemberLUBTableIsLoadBearing`(ON=`Member var1` / TYPELUB_OFF=首臂 `Method var1`) + `TestTernaryDeclLUBCacheRefreshIsLoadBearing`(ON=`Member var1` / CACHE_OFF=陈旧 `Method var1`), 种子 `ReflectMemberLUBSeed.class`(`--release 8`, `Member m = method!=null?method:field`) |
| **方法引用实例化 functional-interface 类型上行(治本)** | `var = Collections::synchronizedList`(fastjson2 ObjectReaderImpl{List,ListStr,Map,MapMultiValueType} 的 `synchronized*`/`unmodifiable*` 全族, 25 处): 方法引用本身**没有目标类型**, invokedynamic 的 instantiatedMethodType(第 3 个 bootstrap 实参)记录了实例化签名 `(List)List`, 但 `bootstrap_methods.go` 的方法引用分支(不同于内联 lambda 分支)**遗漏**了用它上行类型, 使方法引用值的类型停留在 **RAW `Function`**。于是承载它的局部被声明成裸 `Function`(SAM 为 `apply(Object)`), 重建出的 `Function f = Collections::synchronizedList` 被 javac 拒收(`incompatible types: invalid method reference` —— `synchronizedList(List):List` 无法绑到 `apply(Object):Object`)。治法: 方法引用分支照搬内联 lambda 分支的 `inferLambdaTypeFromInstantiated(typ, args1[2])` 上行(仅 7 个标准 JDK functional interface 精确匹配, 其余返回 nil 保 RAW), 使值类型升为 `Function<List, List>`; 槽据此声明成参数化 `Function<List, List> f = Collections::synchronizedList`(声明-带-初值分支天然采用值类型, 无需额外造型), 方法引用即可绑定。安全: 复用既有 lambda 上行原语, 仅对 7 个已知 functional interface 产出**可命名**参数化类型(其余保 RAW, 行为不变); 升级后的精确类型同时让调用接收者造型(`((Function<List,List>)(ref)).apply(...)`)更精确; A/B 全 jar 实证零回归 | `JDEC_METHODREF_INSTANTIATED_TYPE_OFF` | **fastjson2 tree 218→195**(delta -23, A/B `OFF-ON=+23`; `invalid method reference` 桶 26→1, 仅剩 `Throwable::setStackTrace` 构造器实参一处), guava 200 / codec 0 / gson 1 **delta 全 +0(零回归)**。承重 `TestMethodRefInstantiatedTypeIsLoadBearing`(ON=参数化 `Function<List, List>` 局部 / OFF=复现 RAW `Function`), 种子 `MethodRefInstantiatedSeed.class`(`--release 8`, `Function<List,List> f = Collections::synchronizedList` + 分支重赋, 防内联保留局部声明) |
| **泛型类构造器 + 方法引用: 类字面量多余 `(Class)` 造型 + RAW `new` 缺菱形 `<>`(治本, 两耦合)** | fastjson2 `new ObjectReaderImplFromString((Class)(Duration.class), Duration::parse)` 及 URI/Charset/Pattern/ZoneOffset/ZoneId/TimeZone 同族: 构造器是 `<T> ObjectReaderImplFromString(Class<T>, Function<String,T>)`。两个耦合根因——(1) 类字面量 `Duration.class` 实为 `Class<Duration>`, 但 `JavaClassValue.Type()` 报告**被表示类** `Duration`(为使其作静态调用接收者时渲染成 `Duration.staticMethod()` 而非 `Class.staticMethod()`), 实参造型逻辑见「`Duration` ≠ 形参裸类型 `Class`」便补 `(Class)` 原始造型, **擦除泛型**; (2) 泛型类被渲染成 RAW `new ObjectReaderImplFromString(...)`(无菱形), 把 `Function<String,T>` 形参擦成原始 `Function`。二者叠加使 javac 无法从类字面量推断 T, 方法引用 `Duration::parse` 对原始 `Function` 的 `apply(Object)` 不可绑 → `incompatible types: invalid method reference`。治法: (1) `classLiteralArgToClassParam` —— 实参是类字面量(`*JavaClassValue`)且形参裸类型为 `java.lang.Class` 时**丢弃**该造型(类字面量本就满足任意 `Class<...>` 形参, 字节码已类型校验过); (2) `genericCtorDiamond` —— `NewExpression.String` 对「`SiblingClassSig` 证实为泛型类」且「至少一个方法引用/lambda 实参(`*CustomValue` Flag==`lambda`)」的 `new` 补菱形 `<>`, 让 javac 从构造器实参推断类型参数(`Class<Duration>`→T=Duration→`Function<String,Duration>`, 方法引用即可绑)。安全: (1) 严格限「类字面量实参 × `Class` 形参」, 不碰任何其它造型; (2) 仅「泛型类 × 方法引用/lambda 实参」补菱形——普通 RAW `new` 的 unchecked 警告行为**故意保留**(这些行本就能编译, 不动), 非泛型类绝不补(菱形非法), 已带 `<...>` 的名不重复补; 那些本就 `invalid method reference` 失败的行才被本修复改成可编译, 单调安全 | `JDEC_CLASSLIT_ARG_NOCAST_OFF` / `JDEC_CTOR_DIAMOND_OFF` | **fastjson2 tree 229→218**(delta +11, A/B `OFF-ON=+11`; `invalid method reference` 桶 33→26 + 连带 `incompatible parameter types in lambda` 11→9), guava 200 / codec 0 / gson 1 **delta 全 +0(零回归)**。承重 `TestGenericCtorDiamondIsLoadBearing`(ON=`new CtorDiamondBox<>(` / OFF=raw `new CtorDiamondBox(`) + `TestClassLiteralArgNoCastIsLoadBearing`(ON=裸 `Integer.class,` / OFF=`(Class)(Integer.class)`), 种子 `CtorDiamondSeed.class`+`CtorDiamondBox.class`(`--release 8`, diamond 源编译, 字节码与 RAW 同) |
| **合成 access-bridge 构造器空体丢 `this()` 委派(治本)** | javac 为「跨 nest 访问私有无参构造器」(pre-nestmates `--release 8`)合成 package-private 桥 `C(C$N marker)`(末参是匿名 marker 类), 字节码即 `aload_0; invokespecial C.<init>:()V`(= `this()` 委派回同类私有无参)。反编译把该无参 `this()` 当隐式委派**剥成空体**; 嵌套类扁平成顶层 `Outer$Inner` 单元后, 空体让 javac 补隐式 `super()` → 父类无参构造器。基类(extends Object)因隐式 `super()`==`Object()` 侥幸编过, 但**子类**(`AbstractFuture$UnsafeAtomicHelper`/`$SynchronizedHelper`/`AggregateFutureState$SynchronizedAtomicHelper` 都 extends 私有无参的 `AtomicHelper`)的隐式 `super()` 落到父类**私有** `AtomicHelper()` → `constructor ... has private access`。治法: `DumpMethodWithInitialId` 的 `writeBlock` 对**空体 marker-only 合成桥构造器**(`isSyntheticAccessBridgeCtor` + 单参=marker)补回忠实的 `this();` 委派(桥按构造必委派回同类私有无参, 故 `this()` 恒可解析且语义等价)。安全: 严格限 marker-only 单参桥(arg-forwarding 桥 `C(int,C$N){this(x)}` 绝不被误渲成无参 `this()`); 基类一并改 `this()` 行为不变(经同类无参→Object()) | `JDEC_SYN_BRIDGE_THIS_OFF` | **guava tree 203→200**(delta +3, A/B `OFF-ON=+3`; 清 3 个 `has private access`, `AbstractFuture$UnsafeAtomicHelper`/`$SynchronizedHelper`/`AggregateFutureState$SynchronizedAtomicHelper` 3 单元清零, blockerUnits 131→128), codec 0 / fastjson2 229 / gson 1 **delta 全 +0(零回归)**。承重 `TestSynBridgeThisIsLoadBearing`(ON=桥体含 `this();` / OFF=空体), 种子 `SynBridgeSeed$Sub.class`(+`$Base`/`$1`/外层, `--release 8` 强制合成 marker 桥) |
| **`Enum<E>.compareTo(E)` 实参多余 `(Enum)` 上行造型(治本)** | `state().compareTo(State.RUNNING)`(guava `AbstractService$HasReachedRunningGuard`/`$IsStoppableGuard`/`ServiceManager$ServiceManagerState`): `java.lang.Enum<E>.compareTo(E)` 的描述符把类型变量 E 擦除为其 bound `java.lang.Enum`, 实参造型逻辑见「实参静态类型 `Service$State`(具体枚举) ≠ 形参描述符 `java.lang.Enum`」便补 `(Enum)` 上行造型 → `compareTo((Enum) State.RUNNING)`, 破坏 compareTo(E) 真实签名, javac 报 `Enum cannot be converted to Service$State`。这是「擦除型类型变量多余 upcast」族(`JDEC_NO_ERASED_TYPEVAR_NOCAST`)在 **JDK 被调**上的遗漏: `Enum.compareTo` 的 bound 是 `Enum`(非 `Object`, 故既有 Object 闸门不拦), 且 JDK 类对 `SiblingClassSig` 不可见。治法: 新增 JDK 伴生 `jdkCalleeParamIsErasedTypeVar`(描述符键控)—— 当方法名 `compareTo`、单参、形参 0 的描述符类型恰为 `java.lang.Enum` 时, 判定为擦除型类型变量, 让实参造型逻辑**丢弃**该上行造型, 由精确实参类型驱动 javac 推断 E。安全: 实参本就是流入该形参的同一具体枚举(必可赋), 丢造型行为保持; 仅在三个泛型解析器都未复原具体类型时介入 | `JDEC_ENUM_COMPARETO_NOCAST_OFF` | **guava tree 206→203**(delta +3, A/B `OFF-ON=+3`; 清 3 个枚举转换错, `AbstractService$HasReachedRunningGuard`/`$IsStoppableGuard`/`ServiceManager$ServiceManagerState` 3 单元清零, blockerUnits 134→131), codec 0 / fastjson2 229 / gson 1 **delta 全 +0(零回归)**。承重 `TestEnumCompareToNoCastIsLoadBearing`(ON=裸 `compareTo(State.RUNNING)` / OFF=复现 `(Enum)(State.RUNNING)` 上行造型), 种子 `EnumCompareToSeed.class`(`--release 8`) |
| **注解值里外部嵌套枚举常量扁平名(治本)** | `@ReflectionSupport(value=ReflectionSupport$Level.FULL)`(guava `AbstractFuture`/`AggregateFutureState`/`InterruptibleTask`): 注解值是嵌套枚举常量 `j2objc 的 ReflectionSupport.Level.FULL`, 字节码存其二进制描述符 `Lcom/google/j2objc/annotations/ReflectionSupport$Level;`。注解渲染器(`DumpAnnotation` 内联 `parseElement` 与 `formatAnnotationElementValue` 两处 `'e'` 分支)走**裸字符串切片**, 留下扁平 `$` 名 → 同时产出非法 `import ...ReflectionSupport$Level;` 与不可解析的 `ReflectionSupport$Level.FULL`, javac 报 `an enum annotation value must be an enum constant`(+ import `cannot find symbol`)。治法: 新增 `externalNestedEnumSourceName` —— 当枚举宿主是**嵌套类型**且 `foldSiblingResolver` **miss**(证明非反编译同胞、而是外部依赖/JDK 嵌套类)时, 改写成点号源名 `ReflectionSupport.Level.FULL` 并只 import 外层 `ReflectionSupport`。安全: 反编译同胞(扁平成独立 `Outer$Inner.java` 单元, resolver 命中)保留 `$`; 无 resolver(单类模式)保留旧行为; 匿名/局部段(`Outer$1`)守门跳过 | `JDEC_ANNO_ENUM_NESTED_DOT_OFF` | **guava tree 215→206**(delta +9, A/B `OFF-ON=+9`; 清 3 个注解值错 + 连带非法 import 错, `InterruptibleTask` 单元清零, blockerUnits 135→134), codec 0 / fastjson2 229 / gson 1 **delta 全 +0(零回归)**。承重 `TestAnnoEnumNestedDotIsLoadBearing`(永远 miss 的 resolver 模拟外部依赖, ON=点号 `AnnoEnumAnno.Kind.FULL` / OFF=扁平 `AnnoEnumAnno$Kind.FULL`), 种子 `AnnoEnumSeed.class`(`--release 8`, 同胞 `AnnoEnumAnno{,$Kind}.class` 已删以模拟外部) |
| **内在布尔值赋 int 缺 `? 1 : 0` 造型(治本)** | 源码 `int x = (boolExpr) ? 1 : 0;` 里 `boolExpr` 是**非短路布尔连接** `c1 & c2` / `c1 \| c2` / `c1 ^ c2`(或布尔三元): 因连接结果已是 0/1, 旧版 javac(guava 28.2 构建期)把它**留在操作数栈上直接 `istore`**(无分支), `? 1 : 0` 被省去。JavaJive 据此复原出 `int x = c1 & c2`(`JavaExpression.boolConnectiveConds` 使其 `Type()` 为 boolean), 赋给 int 量 → javac `boolean cannot be converted to int`(guava `DoubleMath`/`LongMath.log2`, `ImmutableSortedMap.copyOfInternal`)。治法: `AssignStatement.String` 走 `values.CoerceIntAssignRHS` —— 当左值为 int 且右值是**内在布尔值**(`IntrinsicBooleanValue`: 布尔连接表达式 或 双臂皆 boolean 的三元, **非**裸 ref/slot)时, 重新包成 `(rhs) ? (1) : (0)`。安全: 仅命中「int = 内在布尔」——这种赋值现状**必然**编不过, 故修复单调安全(只把报错行改成可编译, 绝不动现已编过的行); 严格排除**裸布尔型 ref**(`LocalCache$Segment` 的 `this.count = var13`, var13 是被错并成 boolean 实则持 int 的槽——属 disjoint 槽复用分裂另案), 避免静默误编译 | `JDEC_BOOL_TO_INT_COERCE_OFF` | **guava tree 219→215**(delta +4, A/B `OFF-ON=+4`; 清 `DoubleMath`/`LongMath` 两单元 + `ImmutableSortedMap` 一行), codec 0 / fastjson2 229 / gson 1 **delta 全 +0(零回归)**。承重 `TestBoolToIntCoerceIsLoadBearing`(钉 guava `DoubleMath` 真 jar, ON=2/OFF=4; 注: 现代 javac 9+ 恒以分支物化 `bool?1:0`, 该无分支 `iand;istore` 形态无法用本机工具链从源码生成, 故以真 jar 钉) |
| **try/catch 兄弟臂 boolean phi 合并(治本)** | `boolean b; try { ...; b = true; } catch (...) { b = false; } USE(b);`(gson `SqlTypesSupport.<clinit>`): javac 把 `b = true/false` 编成 `iconst_1/iconst_0; istore`(int 范畴), 两臂(try 体 vs catch 处理块)**互不相交**, 谁都不到达谁——无支配性默认初始化供既有 `reachingBoolDefaultMerge` 锚定。DFS 先访问一臂铸出 int 版本, 合流读绑定它, 之后的 boolean 用法(`putstatic ...:Z`)把它重定型为 boolean; 第二臂的 int-0/1 存储见 current=<boolean 版本> 但 val=int, `AssignVarGuarded`(int↔boolean 不可转)分裂出新 int 量。第一臂 boolean 版本在合流处被读、却在第二臂路径未定义, 兜底 pass 合成 `Object var1 = null;` → `Object cannot be converted to boolean`。治法: 新增 `reachingBoolSiblingArmMerge`(`reachingBoolDefaultMerge` 的镜像, 角色互换): 当存储值是 int-0/1 字面量、且槽 current 版本已是 boolean、且 phi(下游 load 同时被本存储与 current 的 def 到达)证明同一变量, 把字面量重定型为 false/true 并延续该 boolean 版本(不分裂)。安全: 仅「同名 phi 共享 load」子集, disjoint 槽复用无共享 load 仍分裂 | `JDEC_BOOL_SIBLING_ARM_MERGE_OFF` | **gson tree 2→1**(`SqlTypesSupport` 清零)**+ fastjson2 tree 230→229**(顺带清一处同形态), guava 219 / codec 0 **delta 全 +0(零回归)**。承重 `TestBoolSiblingArmMergeIsLoadBearing`(ON 统一 `boolean var1` 两臂赋 true/false / OFF 复现 `Object var1 = null` 分裂), 种子 `BoolSiblingArmSeed.class`(javac `--release 8`) |
| **null 链式字段赋值临时 Object→Node 存储(治本)** | `n.left = n.right = n.parent = null` 链式赋值: javac 发 `aconst_null` 后 `dup_x1` 复制存三个字段, JavaJive dup 机制把复制值折成共享临时 `Object varN = null`, 再存进 `Node` 字段 → javac `Object cannot be converted to Node`。治法: `checkAndConvertRef` 对裸 `null` 字面量短路不折临时, 留栈上每个存储点重新物化 `null`(可无造型赋任意引用类型) | `JDEC_NULL_DUP_FOLD_OFF` | **gson errLines 11→4**(`LinkedHashTreeMap$AvlBuilder` 清零)。承重 `TestNullDupFoldSuppressionIsLoadBearing`, 种子 `NullDupSeed{,$Node}.class` |
| **`? super E` 消费者实参造型(治本)** | `Predicate<? super E>.apply(element)` 等下界通配符接收者: `ResolveInstantiatedParamType` 遇任意通配符实参一律 bail, 漏掉 `? super X` 这一**可命名**消费者位 → `Object cannot be converted to CAP#1`。治法: 下界 `? super X` 实参放行, 取其下界 X(限类作用域类型变量)作造型目标; 无界 `?`/上界 `? extends X`(捕获成不可命名 CAP#)仍 bail | `JDEC_GENERIC_SUPERWILDCARD_OFF` | **guava errLines 238→219, blockerUnits 146→137**(清 9 类, 含 `Maps$TransformedEntriesMap`)。承重 `TestSuperWildcardArgCastIsLoadBearing`(`DecompileWithResolver` 多类), 种子 `SuperWildcardSeed.class`+`SuperWildcardSink.class`。注: 与 `JDEC_GENERIC_INFER`(Bug AH 泛型实例化)在 `PairwiseEquivalence` 的 "Object→CAP#1" 上**重叠共护**(任一开启即清), 故 `test/cross/TestGenericInstantiationIsLoadBearing` 已改为「全程强制本开关 OFF, 再单独 toggle generic-infer(ON=0/OFF=2)」以隔离观测 |
| **`NATURAL_ORDER` 构造器自调用通配符实参造型(治本)** | `this((Comparator<? super K>) NATURAL_ORDER)`: 源码的 unchecked 造型被泛型擦除, 实参(`NATURAL_ORDER`)与形参(`Comparator<? super K>`)同裸类型 `Comparator` 使既有造型逻辑不触发 → `Comparator<Comparable> cannot be converted to Comparator<? super K>`。治法: `dumper.go` 登记同类构造器 Signature(按实参数, **偏移护栏**: 仅签名形参数==描述符实参数时登记, 跳过非静态内部类合成参数), `ctorWildcardArgCast` 对**真 `this(...)` 自调用**(`IsThis` 护栏防静态工厂 `new C(...)` 越界)在裸类型相同但参数化不同时补造型 | `JDEC_CTOR_WILDCARD_CAST_OFF` | **gson `LinkedTreeMap`+`LinkedHashTreeMap` 构造器类型错清零(blockerUnits 4→2)**, guava 零回归。承重 `TestCtorWildcardArgCastIsLoadBearing`, 种子 `CtorWildcardSeed.class` |
| **扁平内部类外层形参元数对齐(治本)** | 非静态内部类被扁平成顶层 `Outer$Inner` 后, 其**引用点**(外层类里的字段 `final Outer<K,V>.Inner f;`)的泛型签名编码了**完整**外层实参集 `LOuter<TK;TV;>.Inner;`, `parseSigClassType` 把这一整套 `<K, V>` 带到扁平名上; 但用来给扁平内部类声明回填类型变量的**用量扫描**只复原内部类体(超类型 + 字段签名)里**实际提及**的子集 → 声明/引用元数不一致: `KeySet`(只经 `extends AbstractSet<K>` 用 K)声明成 `<K>` 却被引用成 `<K,V>`(`wrong number of type arguments; required 1`), `GsonContextImpl`(K/V 一个都不用)声明成裸名却被引用成 `<T>`(`type ... does not take parameters`; gson `TreeTypeAdapter$GsonContextImpl`)。治法: `dumper.go` 注入块在用量子集算出后, 用新增 `enclosingFormalTypeParamsForArity`(经 `selfInnerClassAccessFlags` 确认是**非静态**内部类 + 沿二进制名 `$` 链 `foldSiblingResolver` 取**最近泛型外层类**的形式参数名)拿到完整有序外层形参 `encl`; 当用量子集 `free ⊆ encl`(`typeNamesSubset`)即整体改用 `encl`, 使声明元数与顺序对齐引用点。安全: 仅非静态内部类(静态嵌套不捕获外层形参); `free ⊆ encl` 闸门把「用了更深外层/方法级变量」的深嵌套留在旧用量路径(残余); 引用点恒带完整外层实参集, 故对齐后绝不产生 "wrong number"; 注入名即便未用也是合法 Java, 裸引用点(raw)仍可编译; 无 resolver(单类反编译)回退旧行为 | `JDEC_INNER_ENCLOSING_ARITY_OFF` | **gson 缺陷外层类 4→3**(清空 `TreeTypeAdapter`, errLines 27→24) **+ guava errLines 290→273**(顺带修一批同形态 `wrong number of type arguments`, 虽未整类清零但逼近), 它 6 jar(codec/lang3/jsoup/snakeyaml/spring/fastjson2)**delta 全 +0**(JavaJive-only A/B 八 jar 实测零回归)。承重 `TestInnerEnclosingArityIsLoadBearing`(必须 `DecompileWithResolver` 多类; 覆盖空用量 `Inner` + 部分用量 `KeyView` 两态, ON 整体 `<K, V>` / OFF 回退裸名与 `<K>` 子集), 种子 `EnclosingAritySeed{,$Inner,$KeyView}.class`(`--release 8`) |
| **dup/dup2 多临时拼接 · 复合数组自增(治本)** | 一条 `dup2` 指令为复合数组存储 `this.f[expr]++` **同时物化两个临时**(数组引用 `this.pathIndices` 与下标 `this.stackSize-1` 都需落临时)时, 连发两条 `AssignStatement`, 两节点共享 `node.Id == opcode.Id`(`statementsIndex` 整条 opcode 固定为 `opcode.Id`)。`idToNode` 只留**最后一个**(数组节点), 图连边 `predecessor → idToNode[opcode.Id]` 只到达数组节点, 较早的下标赋值节点成**孤儿被丢弃** → 其 `JavaRef` 永不被 `RewriteVar` 改名, 与数组临时撞名, 产出 `int[] var1 = this.pathIndices; var1[var1] = var1[var1] + 1`(int[] 当下标, javac 报 `int[] cannot be converted to int`; gson `JsonReader.pathIndices[stackSize-1]++`)。治法: 图连边后(`code_analyser.go`)扫描同 `Id` 的 dup-family 节点组(纯 `AssignStatement` 且组 size≥2), 把孤儿按发射顺序重新拼回主节点之前 `preds → 下标 → 数组 → store`(`ReplaceNext`/`AddSource` 重接边), 后续 `WalkGraph` 重建即纳入孤儿。安全: 严格闸门(仅 dup 家族 opcode + 组内全为赋值语句 + 组 size≥2), 常见单物化 dup(组 size 1)与所有非 dup opcode 零触碰 | `JDEC_DUP_MULTI_TEMP_SPLICE_OFF` | **gson 缺陷外层类 8→6 + fastjson2 50→49**, 它 6 jar 零回归(公平口径 A/B 全量复跑确认)。承重 `TestDupMultiTempSpliceIsLoadBearing`(ON 补回下标临时 / OFF 复现 `varN[varN]` 自下标), 种子 `ArrCompoundSeed.class`(`--release 8`) |
| **通配符返回造型 · 同类调用(治本)** | 泛型方法 `<T> R<T> create()` 返回**同类**另一方法 `R<?> helper()` 的结果(`return this.helper();`)。调用点字节码描述符只剩擦除裸 `R`, JavaJive 模型看不到冲突; 但 javac 用 helper 真实签名 `R<?>` 捕获成 `CAP#1`, 报 `R<CAP#1> cannot be converted to R<T>`(gson `JsonAdapterAnnotationTypeAdapterFactory.create → getTypeAdapter` 返回 `TypeAdapter<?>`)。真源码带 unchecked `(R<T>)` 造型。治法: `typeVarReturnCast` 新增 `FunctionCallExpression` 分支——对 `this.` 同类实例调用, 用 `funcCtx.MethodSignature` 复原被调方法**真实泛型返回**(`ParseMethodSignatureFull`), 若为**同擦除通配符参数化**(`R<?>`)即补 `(R<T>)`。安全: 严格闸门(this 接收者 + 同类方法 + 返回为同擦除通配符), 普通调用(复原自身具体签名)绝不过度造型 | `JDEC_WILDCARD_RET_CAST_OFF` | **gson 缺陷外层类 6→5**, 它 7 jar 零回归。承重 `TestWildcardReturnCastIsLoadBearing`, 种子 `WildcardRetSeed.class`(`--release 8`) |
| **通配符参数化字段存储造型(治本)** | 同类字段声明为 `Class<? super T>`(通配符 + 类型变量), 构造器 `this.rawType = getRawType(this.type)`, getRawType 真实返回 `Class<?>`。调用点只剩擦除裸 `Class`, javac 用真实签名捕获成 `CAP#1`, 报 `Class<CAP#1> cannot be converted to Class<? super T>`(gson `TypeToken`)。真源码带 unchecked `(Class<? super T>)` 造型。治法: `AssignStatement` 字段存储路径新增 `wildcardFieldStoreCast`——用 `funcCtx.FieldSignature` 复原字段参数化声明类型(`ParseSignature`), 若为**含通配符且提及类作用域类型变量的同擦除参数化**且 RHS 擦除到同一裸类型(且非已是该参数化), 补字段声明类型造型。安全: 造型恒为 unchecked-legal(值擦除到字段裸类型, 永不 inconvertible); 严格闸门把全具体/非类型变量泛型字段排除 | `JDEC_WILDCARD_FIELD_CAST_OFF` | **gson 缺陷外层类 5→4 + fastjson2 49→48**, 它 6 jar 零回归。承重 `TestWildcardFieldStoreCastIsLoadBearing`, 种子 `WildcardFieldSeed.class`(`--release 8`) |
| **`cast()` 重参数化返回点造型(治本)** | 非静态泛型方法的声明返回类型是**同一个类**但用**方法自己的**类型变量参数化(`<N1 extends N> C<N1> cast() { return this; }`)。`this` 是 `C<N>`(类自身参数化)而非 `C<N1>`, 真源码带一个擦除后无字节码的 unchecked `(C<N1>) this` 造型, 历史 `typeVarReturnCast` 在同擦除的 `return this` 上一律不造型(理由: 类自身类型靠 unchecked conversion), 漏掉这条 → javac 报 `C<N> cannot be converted to C<N1>`(guava `GraphBuilder`/`NetworkBuilder`/`ValueGraphBuilder`/`ElementOrder.cast()`, 4 个整类各 1 行)。治法: `typeVarReturnCast` 的 `return this` 同擦除分支不再无脑跳过, 改为「仅当返回类型的**类型实参列表 == 类自身类型参数**(`returnArgsAreClassParams`, 取自新加的 `ClassContext.ClassTypeParams` — 类作用域专属、绝不混入方法 `<T>`)才不造型」, 否则补 `(C<N1>) this`。安全: 该造型恒为 unchecked-legal(`this` 永是类型变量参数化, 同擦除异参数化的造型必合法), 且 return 造型不参与重载消解, 结构性零新错; 身份返回(`InnerNode<K,V>` in `InnerNode<K,V>`, 实参==类参数)仍不造型(守住 InnerNode 过度造型回归) | `JDEC_THIS_REPARAM_CAST_OFF` | **guava tree 365→360(delta +5, A/B `OFF-ON=+5`), blockerUnits 171→167(-4), 缺陷外层类 94→90**(清空 `ElementOrder`/`GraphBuilder`/`NetworkBuilder`/`ValueGraphBuilder` 4 整类), 它 jar 零回归(公平口径 benchmark 全量复跑确认)。承重 `TestThisReparamReturnCastIsLoadBearing`(单类 Decompile, ON 补造型 + 身份 self() 不造型 / OFF 回退裸 `return this;`), 种子 `ThisReparamSeed.class/.golden`(`--release 8`) |
| **扁平内部类丢外层类型参数 bound(治本)** | 非静态内部类继承外层类的类型变量, 被扁平成顶层 `Outer$Inner` 单元后, 历史注入逻辑(`JDEC_INNER_TYPEVAR`)虽**重建了变量名**却把 bound 一律默认成 `Object`(裸 `<C>`)。于是把 `C` 用在需要 bound 的位置——`Range<C>`(guava `Range<C extends Comparable>`)——javac 拒为 `type argument C#1(无界) is not within bounds of type-variable C#2(Comparable)`。这是 guava `not within bounds` 阻断族(`TreeRangeSet`/`TreeRangeMap`/`ImmutableRangeSet`/`ImmutableRangeMap`, 4 个外层类 / 25 个扁平单元 / ~90 行)。治法: 新增 `types.ClassFormalTypeParamBounds`(解析类签名形式类型参数的 bound 子句 + 其引用的类型变量) + `ClassObjectDumper.enclosingTypeParamBounds`(沿二进制名 `$` 链 `Outer$Mid$Inner→Outer$Mid→Outer` 用 `foldSiblingResolver` 加载**外层类**字节, 取每个被注入变量的真实 bound, 重建成 `<C extends Comparable<?>>`)。安全: 仅当(a)外层声明有**非 Object** bound 且(b)bound 引用的每个类型变量**都在**被注入的 free 集里(否则裸名兜底, 永不引用未声明变量)才补 bound; 最近外层作用域优先(类型变量遮蔽); 只改**声明**(加 `extends`)、不动引用点元数, 故绝不引入 "wrong number of type arguments"; 无跨类 resolver(单类反编译)或开关置位时回退裸名 | `JDEC_INNER_TYPEVAR_BOUND_OFF` | **guava tree 478→365(delta -113, A/B `OFF-ON=+113`), blockerUnits 190→171(-19)**, codec/commons-lang3/fastjson2/jsoup/snakeyaml/spring **delta 全 +0**(A/B `TestJarRecompileDelta` tree 七 jar 实测零回归)。修复整桶清空 `type argument ... is not within bounds`(54+31+5=90 行)并连带 `method invocation cannot be applied` 46→34、`incompatible types` 202→189。承重 `TestInnerTypeVarBoundIsLoadBearing`(必须 `DecompileWithResolver` 双类: 内部类 + 外层类), 种子 `ITVBoundSeed$Ranges.class`+`ITVBoundSeed.class`(`--release 8`); bounds 解析单测随 `types` 包 | 
| **擦除型类型变量的多余 upcast 抑制(治本)** | 调用泛型方法时, 若被调形参是它自己签名里的**类型变量** `C`(描述符把它擦成 bound, 如 `<C extends Comparable> Range<C> closed(C,C)` → `closed(Comparable,Comparable)`), 实参渲染器见「实参静态类型 `Integer` ≠ 形参描述符类型 `Comparable`」便补 `(Comparable)` 造型。但实参既已沿字节码流入该形参, 必然可赋给 bound, 故这是**无操作的向上造型**——它却让 javac 在调用点把 `C` 推断成 `Comparable` 而非精确的 `Integer`, 进而 `ContiguousSet.create(Range<C>, DiscreteDomain<Integer>)` 不再适用(`method create cannot be applied`)。治法: 新增 `FunctionCallExpression.calleeParamIsErasedTypeVar`——经 `SiblingClassSig` 取被调(JAR 内)类的方法 Signature, 若第 i 形参是被调**方法自身**或**其声明类**的形式类型参数(`MethodFormalTypeParamNames`/`ClassFormalTypeParamNames`), 即判定为擦除型类型变量, 让实参造型逻辑**丢弃**该造型, 由精确实参类型驱动推断。安全: 仅当三个泛型解析器(`instantiatedParamType`/`sameClassMethodParamType`/`resolvedParamType`)**都未**复原出具体/可命名类型时才介入(解析器复原的造型是要保留的); JDK 被调(`SiblingClassSig` miss)留在描述符上不动; 抑制的恒为向上造型(字节码已保证可赋), 不改变可赋性, 唯一理论风险是重载消歧, 经 A/B 全 jar 实证零回归 | `JDEC_NO_ERASED_TYPEVAR_NOCAST` | **guava tree 492→478(delta -14, 归一化 diff 14 修复/0 新错)**, codec/fastjson2/spring 零回归(A/B `TestJarRecompileDelta` 四 jar delta 全 +0)。修复横跨 `ContiguousSet`/`Range`/集合家族的 `(Comparable)` 假造型; `method invocation cannot be applied` 桶 50→46。承重 `TestErasedTypeVarCastSuppressionIsLoadBearing`(ON 丢造型 / OFF 回归, 必须 `DecompileWithResolver` 双类), 种子 `ErasedTV.class`+`ErasedTVUser.class`(`--release 8`)。**残余**: 被调方法在 `SortedLists.binarySearch` 这类**同名同元重载**上时, `buildSiblingClassSig` 按 (name,arity) 冲突丢弃签名, 故抑制未命中(约 6-8 行); 需描述符级消歧, 留下一轮 |
| **构造器方法引用渲染 `::new_` → `::new`(治本)** | `Type::new` 的 invokedynamic impl 句柄是 `REF_newInvokeSpecial` → `<init>`, 渲染器把成员名设为 `new` 后**误过了 `SafeIdentifier`**——该函数把关键字 `new` 改写成 `new_`, 于是输出 `Type::new_`。javac 视之为「invalid method reference」(去找一个名为 `new_` 的方法, 不存在)。治法: 成员为 `<init>` 时直接返回 `funcCtx.ShortTypeName(impl)+"::new"`, 让关键字 `new` 绕过 `SafeIdentifier`(它只该清洗普通标识符) | `JDEC_CTOR_METHODREF_FIX_OFF` | **fastjson2 tree 248→242(delta -6, 归一化 diff 9 修复/3 推进到更深的 functional-interface 造型错——非回归, 原行就已失败)**, codec/guava/spring 零回归(A/B 四 jar delta 全 ≥0)。承重 `TestCtorMethodRefIsLoadBearing`(ON=`::new` / OFF=`::new_`), 种子 `CtorMethodRefSeed.class/.golden`(`--release 8`) |
| **泛型擦除缺造型 · 统一跨类解析器(治本)** | 前几行的 JDK 写死表 / 同类方法 / 直接超类型恒等一层都是**特例拼盘**, 漏掉三类残余: (i) 接收者是本类型的**局部/字段**而非 `this`(`var0.setCount(objVal)`, var0=`Multiset<E>`); (ii) 超类型**非恒等**实参映射(`Sub<X,Y> implements Super<Y,X>` 换序 / 具体化); (iii) 方法声明在**深链**祖类/祖接口。治法(参照 CFR/Vineflower 的「类型模型+替换+传播」): 新增纯函数替换引擎 `types.SubstituteTypeVars(t, σ)`(递归改写类型变量/参数化实参/数组元素) + 统一原语 `types.ResolveInstantiatedParamType` —— 给定接收者参数化类型 `C<A..>` 沿其泛型超类型层级 DFS, 在每条 `extends/implements Super<...>` 边上**复合类型实参替换 σ**(支持换序/具体化, 非仅恒等), 命中被调方法的泛型 Signature 后施加 σ 取第 i 形参, 喂给既有实参造型。跨类签名经 dumper 顶层闭包 `buildSiblingClassSig`(复用 `foldSiblingResolver`+`Parse`+缓存, 仅传字符串签名)下沉到 `ClassContext.SiblingClassSig`, 避免 types↔parser 循环依赖。安全: **附加式** —— 仅当 JDK 表(`instantiatedParamType`)与同类路径(`sameClassMethodParamType`)**都返回 nil** 才调用, 产物是旧行为超集; 可造型闸门只放行**当前类类作用域类型变量**(`IsTypeParam`, → `(K)`)或**具体(带点)类名**(→ `(Foo)`), 方法作用域 `<T>` / 残留外来裸类型变量 / 通配符捕获 / 参数化·数组结果一律返回 nil(不可命名则不造); JDK/外部超类型(resolver miss)留给 JDK 小表 | `JDEC_GENERIC_RESOLVE_OFF` | **guava tree 522→492(delta +30, 0 新错)**, codec/fastjson2/spring 零回归(A/B `TestJarRecompileDelta` 自身计数下 guava +15、它 jar +0)。归一化 diff 实证: 30 条修复全在 `Object cannot be converted to E/N/K/V/C/B` 桶, 横跨 collect/graph/hash/util.concurrent 14 个真实类; blockerUnits 202→194。承重 `TestGenericResolveArgCastIsLoadBearing`(三场景 ON/OFF 双断言, 必须 `DecompileWithResolver` 多类), 种子 `DeepThisSeed/GenA/GenB/SwapSeed/Pair/FieldRecvSeed/Box.class`(`--release 8`); 替换引擎单测 `TestSubstituteTypeVars` |
| **继承超类型泛型方法实参造型 · this 接收者** | `this.get(objVal)` —— 被调方法**声明在直接超类型**(接口/父类)而非本类(guava `AbstractLoadingCache` 抽象类 `this.get(k)`, get 来自接口 `LoadingCache<K,V>`)。本类 `MethodSignatures` 只含自有方法, 故同类方法造型未命中, javac 报 `Object cannot be converted to K`。治法: 反编译本类时用 `foldSiblingResolver`(jar 路径已具备的跨类字节加载)加载**直接超类型**字节, 在**恒等类型实参映射**(`Sub<K,V> implements Super<K,V>`, 每个实参都是与超类型形参同名、且本类自有的类型变量)下, 把超类型方法的泛型 Signature **原样并入**本类 `MethodSignatures`(同 (name,arity) 本类优先, 多超类型冲突丢弃), 复用 `sameClassMethodParamType`。安全: 仅恒等映射(换序/具体实参的非恒等保守跳过, 避免造错类型变量); JDK/外部超类型不在 jar(resolver miss), 由 `InstantiateJDKMethodParam` 路径覆盖, 二者互补; 仅上溯**一层**直接超类型(深链留残余) | `JDEC_GENERIC_SUPER_METHOD_OFF` | **guava 529→522(delta +7)**, codec/fastjson2/spring 零回归。承重 `TestInheritedThisMethodArgCastIsLoadBearing`(必须 `DecompileWithResolver` 双类), 种子 `InheritedThisSeed.class`+`SuperSeed.class`(`--release 8`) |
| **私有同类自有泛型方法实参造型** | `this.updateInverseMap(k, b, objVal, v)` —— 被调方法是当前类**私有**泛型方法(`private void updateInverseMap(K,boolean,V,V)`)。私有方法在 Java 8 字节码里走 `invokespecial`(与 `super.m()` **同一指令**), 上一行的同类方法造型为避免把 `super.m()` 误判成同类调用而**一刀切跳过所有 invokespecial**, 连私有同类方法一并漏掉 → `Object cannot be converted to V`(guava `AbstractBiMap.updateInverseMap`/`removeFromBothMaps` 等家族)。治法: invokespecial 仍按**目标类**区分 —— 目标类==当前类(`f.isCurrentClass`)即私有同类调用, 其签名就在 `funcCtx.MethodSignatures`, 照常补造型; 目标类!=当前类才是 `super.m()`, 跳过 | `JDEC_GENERIC_SELFMETHOD_PARAM_OFF`(总) / `JDEC_GENERIC_SELFMETHOD_PRIVATE_OFF`(仅私有扩展) | **guava 550→529(delta +21)**, codec/fastjson2/spring 零回归。承重 `TestPrivateSelfMethodArgCastIsLoadBearing`, 种子 `PrivateSelfMethodArgCast.class/.golden`(`--release 8` 编译以走 invokespecial) |
| **返回-嵌入赋值局部的声明合成** | 唯一定义是嵌入在条件里的赋值 (`if ((var2 = parse(...)) == null){}else{return var2;}`) 的局部, 被发成**无声明** → `cannot find symbol`(fastjson2 `JSONReaderJSONB` 的 `readLocalDateTime12/14/16` 等日期解析家族)。两处耦合根因: (1) `generatedLocalDeclRe` 把 `return varN` 误当声明(关键字 `return` 命中其类型标识符分支), 使补声明网误以为已声明而跳过; (2) 即便补声明, 因 RHS 是跨类调用(`DateUtils.parseLocalDateTime12`)无符号表可推断返回类型而退化成 `Object`(再触发 return 类型不符)。治法: 收集已声明集时跳过关键字开头的伪声明; 对被 `return` 的未声明局部, 以**所在方法的返回类型**声明(JLS 14.17 返回值必可赋给返回类型, 初值 null + 条件内赋值在 return 前必达) | `JDEC_RETURN_DECL_FIX_OFF` | **fastjson2 285→248(delta +37)**, codec/guava/spring 零回归。承重 `TestReturnLocalDeclSynthesisIsLoadBearing`, 种子 `ReturnLocalDeclSynthesis.class/.golden` |
| **同类自有泛型方法实参造型** | `this.tailSet(var1)` —— 被调方法是**当前类自己声明**的泛型方法(`SortedSet<E> tailSet(E)`), 描述符把形参擦除为 bound(Object), 且其泛型签名位于**同类的另一个方法**上, 故 §字段/JDK 实参造型都未命中, javac 报 `Object cannot be converted to E`。这是泛型擦除阻断**当前最大剩余块**(guava `Forwarding*`/集合家族 `tailSet/headSet/subSet(E)` 等极其普遍)。治法: `ClassContext.MethodSignatures` 按 `(name, arity)` 一次性登记同类方法的原始泛型 Signature(同名同元重载丢弃以防误判); `FunctionCallExpression.sameClassMethodParamType` 在调用点 `ParseMethodSignatureFull` 还原形参类型, **仅对类作用域类型变量**(`funcCtx.IsTypeParam`)造型 —— 绝不碰方法作用域 `<T>`(调用点不在其作用域, `(T)` 不可编译)或具体类型, 喂给既有实参造型基建 | `JDEC_GENERIC_SELFMETHOD_PARAM_OFF` | **fastjson2 307→285(delta +22) + guava 634→550(delta +84)**, codec/spring 零回归。承重 `TestGenericSelfMethodArgCastIsLoadBearing`, 种子 `GenericSelfMethodArgCast.class/.golden` |
| **JDK 泛型方法实参造型 · 字段接收者** | `this.function.accept(var1, var3)` —— 接收者 `this.function` 是同类字段 `BiConsumer<T,V>`, 但 getfield 的字段访问值只带**擦除描述符**(raw `BiConsumer`), 故上一项的接收者参数化造型未命中, javac 报 `BigDecimal cannot be converted to V`。这是泛型擦除阻断的**最大剩余块**(fastjson2 `FieldReader{BigDecimal,BigInteger}Func` 家族 + guava)。治法: `ClassContext.FieldSignatures` 一次性登记同类**参数化字段**的原始 Signature(`Ljava/util/function/BiConsumer<TT;TV;>;`); `FunctionCallExpression.receiverParamTypeArgs` 在调用点按需 `types.ParseSignature` 还原接收者类型参数, 喂给 `InstantiateJDKMethodParam` 即复用既有实参造型基建。字段值类型本身不动(零字段类型涟漪) | `JDEC_GENERIC_PARAM_FIELD_OFF`(字段旁路) / `JDEC_GENERIC_PARAM_INFER_OFF`(全部实参推断) | **fastjson2 332→307(delta +25) + guava 647→634(delta +13)**, codec/spring 零回归。承重 `TestGenericFieldArgCastIsLoadBearing`, 种子 `GenericFieldArgCast.class/.golden` |
| **JDK 泛型方法实参造型 · 值接收者** | 传给 `Map.put(K,V)` / `BiConsumer.accept(T,V)` / `Collection.add(E)` 等的实参, 形参被描述符擦除为 `Object`, 旧实现漏掉源码原有的 `(K)`/`(V)` 造型 → javac 报 `Object cannot be converted to K`。`InstantiateJDKMethodParam`(InstantiateJDKMethodReturn 的形参版)按接收者参数化类型还原形参泛型类型, 复用既有实参造型逻辑重发造型 | `JDEC_GENERIC_PARAM_INFER_OFF` | **fastjson2 334→332 + guava 651→647**(delta +2/+4), codec/spring 零回归。承重 `TestGenericArgCastIsLoadBearing`, 种子 `GenericArgCast.class/.golden`。命中参数化的 local/参数/this 接收者(字段接收者见上一行) |
| **返回点 Object 向下造型** | 方法声明返回具体引用类型, 但返回值静态类型是被擦除的 `Object`(泛型擦除 / try-with-resources 的 null-only 槽, 如 `JSON.parseObject`)。旧实现发 `return objVar;` → javac 报 `Object cannot be converted to JSONObject`。`objectReturnDowncast` 补合法且行为保持的 `(T)` 造型(Object 是一切引用类型的父类, 向下造型恒合法, 与 CFR/Fernflower 一致) | `JDEC_OBJECT_RET_DOWNCAST_OFF` | **fastjson2 tree 355→334**(delta +21), guava 652→651, codec/spring 零回归。承重 `TestReturnObjectDowncastIsLoadBearing`, 种子 `ReturnObjectDowncast.class/.golden` |
| **pop/pop2 裸值语句** | `aload x; pop` 等死载入被渲染成 `var0;`(JLS 14.8 非语句, 不可编译)。`keepDiscardedStackValue` 对无副作用且非语句的丢弃值(裸 local/常量/类引用)直接 elide | `JDEC_POP_ELIDE_OFF` | **spring tree 14→2**(delta +12), codec/fastjson2/guava 零回归。承重 `TestPopElideIsLoadBearing`, 种子 `SpringCglibEmitUtils.class/.golden` |
| **enum-switch `$SwitchMap` idiom** | 跨类 `switch(Outer$N.$SwitchMap$E[e.ordinal()])` 折回 `switch(e){case CONST}` | `JDEC_NO_ENUM_SWITCH_FOLD` | iso 净 +4, 零回归。承重 `TestEnumSwitchFoldIsLoadBearing` |
| **核心非确定性** | CFG 结构化按 Go map 随机序决策 + import 顺序 → 同输入多产物 | (确定性, 无开关) | 4 jar 整树数跨进程恒定。承重 `TestDecompileIsDeterministic` / `TestRegressionSeedsAreDeterministic` |
| **局部变量数据流** | 到达定义 web + 活跃区间分割, 修声明摆放 | `JDEC_LIVEINTERVAL_OFF` / `JDEC_LIVEINTERVAL_WEB` | fastjson2 tree -324。承重 `TestLiveIntervalSplitIsLoadBearing` |
| **类型层级 / 三元 LUB** | `CommonSuperType` + astore 声明点向上拓宽到双臂 LUB(含反射家族 Method/Field→Member 行 + 加宽时刷新三元 cachedType, 详见上方独立行) | `JDEC_TYPELUB_OFF` / `JDEC_TERNARY_DECL_LUB_OFF` / `JDEC_TERNARY_DECL_LUB_CACHE_OFF` | codec/fastjson2 tree 各 -1 + 反射三元 fastjson2 -6, 零回归。承重 `TestTypeLUBIsLoadBearing` / `TestReflectMemberLUBTableIsLoadBearing` / `TestTernaryDeclLUBCacheRefreshIsLoadBearing` |
| **泛型实例化** | `Iterable.iterator()` / `Iterator.next()` 回填实参类型 | `JDEC_GENERIC_INFER_OFF` | guava iso +2 / tree +12, 零回归 |
| **嵌套类型 public 复原** | 从 `InnerClasses` 取回 `ACC_PUBLIC`, 修跨包不可见 | `JDEC_NESTED_PUBLIC_OFF` | fastjson2 多类可见性修复 |

> 完整 kill-switch 索引见 §5。

---

## 3. 剩余缺陷(tree 口径, 按杠杆从大到小)

> 这些是真正阻碍重打包的缺陷。每项给「计数(error lines)·代表类·样例·初判根因」。可执行工单(复现命令 + 优先级)在根 [`TODO.md`](../TODO.md)。

1. **泛型擦除缺造型 `Object cannot be converted to T/K/CAP#1`** — **最大杠杆。当前 `incompatible types (assignment/return)` 桶: fastjson2 29 + guava 134**(含装箱等非擦除项; fastjson2 经跨作用域孤儿读重放 58→34、再经 Object 超类臂合并 34→29)
   - **已治本八块**: 返回点 Object 向下造型(fastjson2 -21); JDK 泛型方法实参 · 值接收者(fastjson2 -2 / guava -4); JDK 泛型方法实参 · 字段接收者(fastjson2 -25 / guava -13); 同类自有泛型方法实参 · 公有(fastjson2 -22 / guava -84); 同类自有泛型方法实参 · 私有 invokespecial(guava -21); 继承超类型泛型方法实参 · this 接收者(guava -7, 恒等映射一层); 统一跨类泛型解析器(guava 522→492 = -30)——以「类型实参替换 σ + 超类型层级 DFS」一举覆盖原 (a) 三子项; **擦除型类型变量多余 upcast 抑制(本轮治本, guava 492→478 = -14, 0 新错)——丢弃 `(Comparable)` 一类无操作向上造型, 让精确实参驱动调用点推断(`method ... cannot be applied` 桶 50→46)**。**剩余类别**(按当前 `cannot be converted to` 直方图):
     - **(a) 非-this 接收者 / 非恒等映射 / 深链 —— 本轮已治本(统一跨类解析器)**: (i) 接收者是本类型的**局部变量/字段**而非 `this`(`this.box.put(o)`, box=`Box<E>`); (ii) 超类型**非恒等**实参映射(`Sub<X,Y> implements Super<Y,X>` 换序/具体化); (iii) 超类型**深链**(方法在父类的父类/祖接口)。三者统一由 `types.ResolveInstantiatedParamType`(沿接收者泛型超类型 DFS + 逐边复合 σ)+ `types.SubstituteTypeVars` 解决, 附加式落地(JDK 表/同类路径均 miss 后兜底), kill-switch `JDEC_GENERIC_RESOLVE_OFF`。实测 guava -30(归一化 diff 0 新错, 横跨 collect/graph/hash/util.concurrent 14 类), 它 jar 零回归。残余同桶里仍有「接收者本身泛型未被传播复原成参数化类型」的 long-tail(业界 CFR/Vineflower 亦有, 见调用链丢泛型), 留下一轮。
     - **(b) 通配符捕获 `CAP#1`(guava 40)** —— **oracle 实证为内在难 case, 优先级下调**: `this.equivalence.equivalent(a,b)`, 字段类型 `Equivalence<? super T>` 捕获成 `CAP#1`, 实参 Object 不可造 `(CAP#1)`(不可命名)。`TestThirdPartyOracle/guava/Equivalence$Wrapper` **三方(JavaJive/CFR/Vineflower)全部重编译失败**: CFR 发 `Equivalence<? super T> e = this.equivalence;` 仍不可编译; 真源码用 `(Equivalence<Object>) this.equivalence` + `@SuppressWarnings`。方向(若做): 通配符接收者**整体** `<Object>` 参数化造型, 非对实参造型。
     - **(c) 装箱/数值**: `int cannot be converted to Integer` 等(**非擦除, 不可盲目造型**), 按 `Integer.valueOf` 修。

2. ~~**`break outside switch or loop`(fastjson2 31)**~~ — **本轮已治本主因(switch 合并点的非支配前驱误改写成 break, 见 §2)**
   - 例(原): `ObjectReaderImplMap.of:74/79/83`、`JSONReader.java:1148`。根因: `switch` 之前的 `if/else-if` 链各分支前向跳到与 switch **相同的合并点**, `SwitchRewriter1` 给 merge 的所有前驱(含 switch 外的 if 链边)都插了 break → 裸 `break` 无所属 switch。治法: 插 break 前加支配性闸门, 仅 switch 传递支配的前驱(case 体)才插。实测 fastjson2 tree 189→165(-24, 桶 31→0), guava 200→199 顺带, gson/codec 零回归(kill-switch `JDEC_SWITCH_NONDOM_MERGE_BREAK_OFF`, 承重 `TestSwitchNonDomMergeBreakIsLoadBearing`)。
   - **残余**: 如仍有标号 break / 复杂循环-switch 深嵌套把 break 落到循环外的个例(非本「共享 merge」形态), 属循环重建长尾(参见 §3.6), 逐个复现。

3. ~~**泛型边界 `type argument K is not within bounds of type-variable C`(guava 53+31+5 ≈ 89)**~~ — **本轮已治本(扁平内部类丢外层类型参数 bound, 见 §2)**
   - 例(原): `ImmutableRangeMap$1.java:21`、`TreeRangeSet$AsRanges`。根因: 扁平嵌套类型注入了外层类型变量名却丢了其 bound, 引用处 `Range<C>` 实参越界。治法: `enclosingTypeParamBounds` 沿 `$` 链取外层类真实 bound 重建 `<C extends Comparable<?>>`。实测 guava `not within bounds` 三桶(54+31+5=90 行)整体清空, 它 jar 零回归(kill-switch `JDEC_INNER_TYPEVAR_BOUND_OFF`)。
   - **残余**: 同形态里如有「bound 引用了未注入的外层变量」会保守回退裸名(安全, 不补 bound), 属深层跨类重建长尾。

4. **三元 LUB `bad type in conditional expression`(fastjson2 11→5→0 + guava 12)**
   - 根因: `cond ? a : b` 两臂最小公共上界算窄, javac 拒绝。已有 `CommonSuperType` 设施, 需扩表 + 在更多合流点接入。
   - **已治本两支**: (1) **反射家族**(fastjson2 -6): `Member m = cond ? method : field` —— 层级表新增 `Method/Field/Constructor → Member`(接口, 非 AccessibleObject)+ `ternaryDeclLUB` 同步刷新三元 `cachedType`, 清 FieldReader/FieldReaderObject 全部反射三元。(2) **跨类(jar 内)直接子类型**(本轮治本, fastjson2 -6 + guava -3, 见 §2): `Any var6 = null; ...; var6 = cond ? Any.INSTANCE : Any.NOT_ANY`(`Any <: JSONSchema`)—— 新增 `ternaryDeclLUBCrossClass` 经 `ClassContext.SiblingSuperTypes` 解析 jar 内 RAW 父类型, 直接子类型关系下把声明 ref 加宽到上界臂(仅 ResetVarType, 不动共享三元缓存以免回流改窄参数); 清 AllOf/ArraySchema/ObjectSchema 等全部 `Any.INSTANCE/NOT_ANY` 三元(`JDEC_TERNARY_DECL_LUB_CROSS_OFF`, 承重 `TestTernaryDeclLUBCrossIsLoadBearing`)。
   - **残余**: (a) `HashMap var = cond ? new HashMap() : (Map)createInstance(...)`(ImplMapString/ImplListStr)—— 假臂的 `(Map)`/`(Collection)` 是**渲染期造型**而其值 `Type()` 仍是 `createInstance` 的 `Object`, 故 `MergeTypes(HashMap, Object)` 退化回首臂 `HashMap`, 声明过窄; 须让渲染造型反馈到三元类型(或假臂 checkcast 值携带目标类型), 留待; (b) `readArray() : readObject()`(JSONArray/JSONObject LUB)同源泛型/LUB 长尾。

5. **`bad operand type for operator`(fastjson2 14) / ~~`unexpected type`(fastjson2 9)~~ 本轮已治本** — 同一深根因: **活跃区间分裂 / 合流点幽灵版本 / 槽位复用类型混淆**
   - 本轮复现确认这几桶与 `FieldWriter` 的 `Field/Class→Method` 一族(§3.8)同源: 一个字节码 local 槽在**互不相交的活跃区间**里先后承载**不兼容类型**的值, 反编译却合成了**单一变量名 + 单一声明类型**。例: `JSONPathFilter$GroupFilter` 的 `var9` 既声明为 `Iterator` 又被 `(var9) != (0)` 当 int 比较(`bad operand: Iterator,int`)。
   - **本轮已治本一支(`FieldWriter` 反射家族整类清零, fastjson2 -17, 见 §2)**: `FieldWriter.compareTo` 的 `Member m1; if(cond) m1=this.method; else m1=this.field` —— 同槽两不相交臂存**兄弟引用类型** `Method`/`Field`(都 `<: Member`、互无子类型), 汇合处多态 `instanceof`/造型使用, 被 `AssignVarGuarded` 按类型拆成 `Method`/`Field` 两名致 16 行 `inconvertible types`。新增 `reachingRefSlotSiblingArmMerge`(`JDEC_REF_SLOT_SIBLING_ARM_MERGE_OFF`): 兄弟引用 phi 经 `CommonSuperType`(非 Object JDK 表上界)+ `slotDefPhiReachesLoad` 双证后把 current 加宽到 LUB(`Member`)续用。这是 disjoint 槽族**「合并到 LUB」**型(boolean 版 `reachingBoolSiblingArmMerge`、子类型版 `reachingRefSlotPhiMerge` 的兄弟引用补全)。`CommonSuperType` 对只共 Object 的 Map/List/Number/String 派发型复用返回 nil, 故与下面「真错并」长尾天然区分。fastjson2 115→98, 三 jar 零回归。
   - **本轮已治本一支(`unexpected type` 桶 9→0, fastjson2 -9, 见 §2)**: `JSONSchema` 的 `var4`/`var6`/`var7` 既被 `(varN)==(0)`(int)又 `varN instanceof T`(Object) —— 但其根因**不是**槽错并(`rewriteVar` 其实已把两活区各 mint 一个身份并正确重绑了 store 端), 而是 `instanceof` 操作数被 `OP_INSTANCEOF` 的 `CustomValue` 闭包冻结、漏传 `replaceFunc`, 末期 `ReplaceVar` 到达不了它致渲染期撞名(`var6 instanceof Integer` 的 `var6` 落到不相交的 int 局部上)。补 `replaceFunc` 转发即正名, `unexpected type` 桶清零(`JDEC_INSTANCEOF_REPLACEVAR_OFF`, 承重 `TestInstanceofReplaceVarIsLoadBearing`)。这说明同一槽多类型表象下**有真错并、也有纯渲染期身份冻结**两种子根因, 须分别甄别。
   - **本轮已治本一支(跨作用域孤儿读, fastjson2 -30, 见 §2)**: `JSONPathParser.parseFilter` 的 16 行 `(String)(JSONPathFunction$SizeFunction)`(表象在 §3.1 `incompatible types` 桶) —— 这是同一槽多类型表象下的**第三类**子根因: slot 7 的 null store 在 `if(fieldName==null)` 子作用域被铸出新 id `var12`, 但读它的构造器调用在**运算符 switch 兄弟作用域**, `rewriteVar` 各作用域只重写自身切片致孤儿读保留旧 id、渲染裸 `var11` 撞上后部 `SizeFunction var11` 声明。既非「真错并」(store 端已正确铸 id)亦非「纯渲染期身份冻结」(`ReplaceVar` 本应到达却因兄弟作用域够不到), 而是**重绑覆盖范围不足**。新增 `Scope.allReplace` 全方法累积 + `replayUnambiguousRebindings` 对唯一映射做全方法重放补绑(`JDEC_ORPHAN_GLOBAL_REBIND_OFF`, 承重 `TestOrphanGlobalRebindIsLoadBearing`), fastjson2 tree 96→66 / guava 190→186, gson/codec 零回归。至此同一槽多类型须甄别**真错并 / 纯渲染期身份冻结 / 跨作用域孤儿读**三类子根因。
   - **本轮已治本一支(disjoint 槽族「Object 超类臂」型, fastjson2 -5, 见 §2)**: `ObjectWriterAdapter.toJSONObject` 的 `String cannot be converted to Map/Collection`(3 行)+ `JSONPathTwoSegment`(2 行同形态)—— 同一槽 current 声明为 `java.lang.Object`(LVT 实证 slot 8 全程一个 `Object fieldValue`), 某不相交臂用更具体引用(`DateUtils.format` 返回的 String)重赋, 汇合处 `instanceof Map`/`(Map)`/`(Collection)` 读绑到被 `AssignVarGuarded` 按类型串拆出的 String 名 → `(Map)(String)` 报错。**Object-current 这条边**前三族都覆盖不到(`CrossClassDirectLUB` 刻意不返回 Object、`CommonSuperType==Object` 命中 sibling 的 "strict sibling only" bail)。新增 `reachingRefSlotObjectArmMerge`(shape A 单向, `JDEC_REF_SLOT_OBJECT_SUPERTYPE_ARM_MERGE_OFF`, 承重 `TestRefSlotObjectArmMergeIsLoadBearing`): current 恰为 Object + store 为具体引用 + phi 时**续用 Object**(绝不改类型, 故无加宽风险——对称 shape B「具体加宽到 Object」实测引发多处 `Object cannot be converted to X`/cfs 回归, 已砍)。fastjson2 tree 66→61, guava/gson 零回归。
   - **本轮已治本一支(disjoint 槽族首个「布尔字段 sink 拆分」型, fastjson2 -4, 见 §2)**: `JSONPathTypedMultiIndexes.<init>` 的 4 行 int/boolean 槽错(`bad operand '<'` + `boolean→int` x2 + `boolean++`)—— JVM slot 11 先作第一个循环的 int 计数器 `i`(活区在循环后死亡), 后复用为 boolean `duplicate`(`=false`/`=true`/`putfield duplicate:Z`); `iconst_0;istore` 的 0/1 字面量被 `AssignVarGuarded` 视同 int 续用、并成单一变量, 槽随后从 `putfield Z` 定型 boolean 致整段 int 循环用法报错。既有 `reachingBoolReturnSlotSplit`(sink=ireturn, 仅 boolean 方法)够不到 **void `<init>`** + 字段 sink 形态。新增 `reachingBoolFieldSlotSplit`(其字段-sink 姊妹, `JDEC_BOOL_FIELD_SLOT_SPLIT_OFF`, 承重 `TestBoolFieldSlotSplitIsLoadBearing`): current 为纯 int 局部 + 存储 int-0/1 + 值前向流入 `putfield/putstatic Z` + **`!slotDefPhiReachesLoad`(与 current def 不共达任一 load=真不相交)** + web 不相交时, 把 0/1 重定型 boolean 拆分。phi 守门是关键判据——它把「真不相交 int 计数器/boolean 标志」(拆)与「同一 boolean 变量 try/catch 两臂汇入同一 `putstatic Z`」(不拆, 留给 `reachingBoolSiblingArmMerge`)严格区分。fastjson2 tree 61→57, guava/gson 零回归。这是 disjoint 槽族第二个**「拆分」**型(boolean-return 之后), 解决了 143 行此前悬测的 `JSONPathTypedMultiIndexes`(实证根因是 int/boolean 双类型槽错并, **非** invokedynamic FuncCtx 形态)。
   - **本轮已治本一支(disjoint 槽族「数组协变父臂合并」型, fastjson2 -3, 见 §2)**: `CSVReaderUTF8/UTF16.readLineValues` 的 `Object cannot be converted to String`(3 行)—— 同一 JVM slot 2(LVT 实证全程单一 `Object[] values`)在 `if(strings)`/`else` 两不相交臂分别存 `new String[]`(协变子类型数组)与 `new Object[]`; DFS 序里 `values=null` 的 null-init 先 null-adopt 采纳 `String[]`(且 `IsNullInitialized()` 终身真、`NullTypeAdopted()` 已置), `new Object[]` 臂遂被 `AssignVarGuarded`(无数组协变规则、adopt-once 已耗尽)拆成独立 `Object[] var4`, 合流读 `values[i]=value`/`return values` 绑到 `String[]` ref 报错。既有 `reachingRefSlot*` 四族**全 bail 在数组上**(`classNameOf` 对数组返 false 致 `CommonSuperType`/`CrossClassDirectLUB` 皆 nil; Object 臂闸门比较 `"java.lang.Object"` 永不等于 `"...[]"`)。新增 `reachingRefSlotArrayCovariantArmMerge`(`JDEC_REF_SLOT_ARRAY_COVARIANT_ARM_MERGE_OFF`, 承重 `TestArrayCovariantArmMergeIsLoadBearing`): 两等维引用数组、元素 LUB 经 `arrayElemLUB`(Object/jar 内/JDK)可证、phi 双证时把 current 加宽到元素 LUB 数组(`String[]→Object[]`)续用。**刻意不门控 `IsNullInitialized`**——CSV current 是 null-adopted ref,由「已是数组类型」隐式保证非标量 Object null-init。fastjson2 57→54, guava/gson 零回归。**残余已治本(本轮, 见 §2「Object→类型变量造型缺失」族)**: CSV 同方法的 `Object[] cannot be converted to T`(`readLineObjectAll` 的 `consumer.accept((T) values)`, UTF8/UTF16 各 1 行)经 `typeVarArrayArgCast`(`JDEC_TYPEVAR_ARRAY_ARG_CAST_OFF`)补出数组→类型变量造型清零, fastjson2 54→52; **至此 CSV 整族(`Object→String` 3 行 + `Object[]→T` 2 行)全清零**。
   - **已治本一支(上轮)**: gson `SqlTypesSupport.<clinit>` 的 `Object cannot be converted to boolean`——try/catch 兄弟臂各赋 boolean(int-0/1)、合流读未定型, 新增 `reachingBoolSiblingArmMerge`(`JDEC_BOOL_SIBLING_ARM_MERGE_OFF`, 见 §2)延续 boolean 版本, gson 2→1 + fastjson2 230→229 零回归。
   - **本轮已治本一支(disjoint 槽族首攻, fastjson2 -16, 见 §2)**: `ObjectWriterProvider.getObjectWriterInternal` 的 `bad operand: String != int`(一处 7 行)——**布尔形参** `fieldBased` 在 `if` 内 `= false`(int-0/1)重赋、随后三元/`if` 读取, 而到达定义是**方法入口形参**(非 store), `reachingBoolSiblingArmMerge` 的 store-only phi 判据漏掉它致分裂出 int 量、汇合读与同深度 `String var6` 撞名。新增 `reachingBoolParamReassignMerge`(形参镜像, `JDEC_BOOL_PARAM_REASSIGN_MERGE_OFF`): 形参臂经 `slotStoreReachesParamPhiLoad`(load 同时无-store 到达入口)证 phi 后续用形参、0/1 转 false/true。fastjson2 161→145, 三 jar 零回归; 实证 `JDEC_LIVEINTERVAL_WEB` 全开对此 0 命中(load 侧因模拟序)且 store 侧净 +2, 故本族须按子形态精确治, 而非全开 web 引擎。
   - **本轮已治本一支(disjoint 槽族首个「拆分」型, guava -6, 见 §2)**: guava `LocalCache$Segment` / `MapMakerInternalMap$Segment`(各 3 行)同一槽先承载 `int newCount = this.count - 1; ...; this.count = newCount;`(int 活区), 再承载 `boolean result = false; return result;`(boolean 活区), 两活区**互不相交**却被并成单一 `boolean var13` → `int cannot be converted to boolean`(922/925)+`boolean cannot be converted to int`(927)。新增 `reachingBoolReturnSlotSplit`(`JDEC_BOOL_RETURN_SLOT_SPLIT_OFF`): 当槽 current 是纯 int 局部、存储是 int-0/1、方法返回 boolean、本 store 前向喂给 ireturn、且 current 的 def 与本 store **不在同一到达定义 web** 时, 把 0/1 重定型为 boolean 使 `AssignVarGuarded` 新铸 boolean 量, 拆开两活区。这是本族首个**「拆分」**方向的精确治本(此前所有 `reachingBool*Merge` 都是「合并」: int-0/1 续到 boolean def)。fastjson2 145 / gson 1 / codec 0 零回归。注: 这与 §2「内在布尔值赋 int」是**两个相反方向**——后者是单槽真持布尔表达式结果(补 `?1:0`), 前者是双类型槽错并(须拆分); 二者均已治本且互不干扰(`CoerceIntAssignRHS` 只认内在布尔值而非裸 ref)。
   - **残余 fastjson2 同源(`JSONPathFilter$GroupFilter` 的 `var9` Iterator/int 等)**: 仍是单槽多类型不相交活区**真错并**(区别于上面 `JSONSchema` 的纯渲染期身份冻结), 类型对(Iterator/int)非「布尔 return」子形态, 须更通用的「区间+类型」拆分核(`JDEC_LIVEINTERVAL_*`), 风险高留专项。`BeanUtils.isWriteEnumAsJavaBean` 的 `return 1/0`(2 行)**已治本**(见 §2「内联 lambda 体懒解析途中污染外围方法 FuncCtx」, `JDEC_LAMBDA_CTX_RESTORE_OFF`): 根因不是 do-while 结构化, 而是同方法内含 invokedynamic lambda, lambda 体在外围解析途中懒 dump 把共享 `FuncCtx` 覆写成 lambda 的 void 且未还原, 致 invokedynamic 之后的尾部 `return false/true` 用 void 做 `resetReturnValueTypeSafe` 不触发。`JSONPathTypedMultiIndexes`(boolean→int 4 行)**本轮已治本**(`reachingBoolFieldSlotSplit`, 见上)——实证根因是 int 循环计数器槽被复用为 boolean `duplicate` 标志(存入 `Z` 字段)的双类型槽错并, 与 invokedynamic/FuncCtx 无关。
   - **prebind 共享槽变体(已回退)**: 曾试 prebind「同名退让」护栏修 gson `LinkedTreeMap.find` 的 `var7_1`(循环局部 `child` 与循环后 `created` 共享 `*JavaRef`), 但它会压掉 fastjson2 `JdbcSupport$TimeReader` 合法的 disjoint 分裂 `long var5_1`(确定性种子 `fastjson2_JdbcSupport_TimeReader.golden` 变红); 「该合并/该分裂」简单「同名」判据无法区分, 已回退。注: `find`/`rebalance` 的 `var_N` 仅在带 gson.jar 的非权威 classpath 下出现, **权威 tree 口径(空 classpath)LinkedTreeMap 实际可编译**, 不在 tree 残留内。
   - **已查明并修复: 2 处先前遗留的 `classparser/` 包测试红, 实为「过时测试夹具」而非真反编译回归(本轮 bisect + 修复)**: 本轮跑全量 `go test ./classparser/` 发现 ① `TestBoolSiblingArmMergeIsLoadBearing` 与 ② `TestDecompileSyntaxRegression/fastjson2_JdbcSupport_TimeReader` 双红。git 逐文件 bisect(备份 working tree → `git checkout HEAD -- <file>` 测 → 恢复)定位**两者均由 `rewrite_var.go` 的先前未提交改动引起**, 进一步 `JDEC_ORPHAN_GLOBAL_REBIND_OFF` 单切定位到 `replayUnambiguousRebindings`(跨作用域孤儿读全方法重放)。**关键结论: 默认全开配置下两个种子的反编译输出都完全正确**——① `BoolSiblingArmSeed.<clinit>` 默认输出正确的 `boolean var1; try{var1=true;}catch{var1=false;} SUPPORTED=var1;`; ② `JdbcSupport$TimeReader` 默认把 slot 5 的**三个**不相交活区各自正确提升定型(`long var5` / `DateTimeFormatter var5_1` / `String var5_2`), 真 jar 在 tree 口径干净可编译(不在 57 残留内)。红的原因是**测试夹具随反编译器改进而失效**: ① `replayUnambiguousRebindings` 对该 try/catch 槽提供了**重叠的防御纵深**, 使 `reachingBoolSiblingArmMerge` kill-switch 单独 OFF 时缺陷**变形**(不再产出 `Object var1 = null;` 幻影、而是另一种 int 拆分), 故承重测试的 OFF 断言失配——治法: 承重测试测量期一并隔离 `JDEC_ORPHAN_GLOBAL_REBIND_OFF=1`(同台账既有「测量期隔离重叠开关」惯例), OFF 即复现 `Object var1=null` 幻影; ② 先前 disjoint 区编号改动把 long 区从 `var5_1` 改名为 `var5`(`var5_1` 让位给 DateTimeFormatter 区), golden 的 `+long var5_1;` 过时——治法: golden 更新为断言三个不相交活区各自正确提升定型(`+long var5;`/`+DateTimeFormatter var5_1;`/`+String var5_2;`, 比原单条规则**更强**的不变量)。**均非打过用例补丁**: 已先核实真 jar tree 干净可编译再改夹具。上轮漏检因仅跑 `./test/cross/ -run IsLoadBearing` + 确定性、未覆盖 `classparser/` 包(今后验收须并跑 `go test ./classparser/`)。
   - 余下 fastjson2 两桶治法须在变量定型/分裂核(`JDEC_LIVEINTERVAL_*`)上按「区间+类型」更激进地拆分同槽 / 合流点同 ref 不 mint, 风险高、改动核心, 留作专项(本轮不动)。

6. **`for` 循环 `continue`-到-自增被丢弃(gson `JsonWriter.string`, tree 唯一残留)** — 循环结构化深根因
   - 现状: gson `JsonWriter.string` 是 `for (i...; i++) { ...; if (repl==null) continue; ...else continue; <写块>; }`。javajive 把 `for` 渲染成 `do-while(true)` 并把自增 `i++` 作为**显式体语句**置于写块后; 于是内层两处 `goto <自增偏移>`(原 `continue`: `repl==null` 与 `c>=128` 非匹配臂)**无法表达为裸 `continue`**(裸 continue 会跳过 `i++`), 结构化遂把它们**丢弃**(渲染成空 if 体 / 缺失的 else), 致 `var7`(repl)未初始化可达 `out.write(var7)` → `variable var7 might not have been initialized`(`JsonWriter.java:264`)。
   - 治本须做 `for` 循环恢复(把自增放进 for-update 槽, 使 `continue` 语义对齐)或等价的「continue-到-latch 自增」结构化, 改动循环结构化核心、影响所有 jar 的循环渲染, 风险高, 留专项。

7. **合成内部类 `this.val$e;` field-read pop(spring 2, 单类 `EmitUtils$6`)**
   - 现状: `pop` 丢弃 `this.val$e`(field read)未被 elide。**已验证不可粗暴扩展**: 把 `RefMember` 纳入 elide 集会引发 spring tree 812 错误的大回归(结构化依赖该节点), 故 §2 的治法只 elide 裸 local/常量/类引用。
   - oracle 旁证(`TestThirdPartyOracle/spring/EmitUtils$6`): **CFR 与 Vineflower 对该合成匿名内部类亦失败**, 属内在难 case, 留长尾, 不为它冒结构化回归风险。

8. **`cannot find symbol`(tree 口径: fastjson2 13 + guava 9)** — 注意这是 **tree(整树)** 残留, 与 §4 的 iso 扁平 `$` 假阳性**不同**。已治本两类(返回-嵌入赋值局部声明合成, fastjson2 -37; 跨作用域孤儿读全方法重放亦顺带 fastjson2 该桶 19→13, 见 §2)。**guava 该桶已由 96 降至 9**: harness 的 `withSunMisc` shim 从运行 JDK 抽取 `sun.misc.*` 上 classpath, 解析掉下面 (a) 整块 sun.misc.Unsafe 一族(约 45 行环境假阳性), 余下经 inner-typevar 等前轮治本收敛。残留分桶后:
   - **(a) `sun.misc.Unsafe` 一族(guava 96 中约 45 行, 环境假阳性, 非缺陷)**: `Striped64`/`LittleEndianByteArray$UnsafeByteArray`/`AbstractFuture$UnsafeAtomicHelper`/`UnsignedBytes$...$UnsafeComparator` 等忠实反编译出 `import sun.misc.Unsafe; ... Unsafe.getUnsafe()`, 但 harness 用 **`javac --release 8`** 编译——其 `ct.sym` 不含 `sun.*` 内部包, 故报 `程序包sun.misc不存在 / cannot find symbol: class Unsafe`(已用最小例 `import sun.misc.Unsafe` 实证: `--release 8` 失败、裸 `javac` 仅警告)。这与 §4 的 iso 假阳性同性质——**任何忠实反编译器(CFR/Vineflower 同样)在 `--release 8` 下都过不了**, 不是 JavaJive 缺陷, 也不阻碍真实重打包(用含 `sun.misc` 的 JDK 编译即可)。已登记到 §4。
   - **(b) 扁平嵌套类丢外层类型参数(`HashIterator`/`Segment`/`Itr` 一族, 约 40+ 行)**: 非静态内部类被扁平成 `Outer$Inner<T>` 后, 其成员引用的外层类型变量 `K,V,E,S` 无处声明 → `cannot find symbol: class K`。`dumper.go` 已对「**自身无形参**」的纯继承内部类注入自由类型变量(`JDEC_INNER_TYPEVAR_OFF`, 历史治本约 2000 行), 但「**自身又有形参**」的 `HashIterator<T>` 是显式残余: 若把外层 `K,V,E,S` 并入声明, 别处对 `HashIterator` 的引用仍只带自有元数, 元数不一致("wrong number of type arguments")。须跨类协同重写所有引用点(integral rebuild), 深且高风险, 留专项。
   - 其余少量为局部命名/分裂不一致、合成 lambda/捕获变量名丢失等, 逐类复现处理。

9. **其余小桶**: `incompatible parameter types in lambda expression`(fastjson2 9→5, 本轮 -4)—— **已治本全未用形参一支**: `rawMap.computeIfAbsent(k, (Integer l0) -> new ArrayList())` 一族(接收者裸 `Map` → raw `Function`), lambda 体未用形参时改隐式渲染 `(l0) -> ...` 让 javac 推断, 见 §2(`JDEC_LAMBDA_IMPLICIT_UNUSED_PARAM_OFF`)。**残余 5**(`ObjectReaderCreator`/`JSONSchema`/`JSONPathSegment$...$MapRecursive`)均为**形参被用作具体类型**(`l0.sameTo(...)`/`l0.fieldClass`)而接收者/目标为裸类型 → 隐式也推不出, 须复原接收者参数化类型(§3.1 接收者泛型传播长尾), 留。`method invocation cannot be applied`(guava 50→46, 经擦除型类型变量造型抑制; 残余以 `SortedLists.binarySearch` 重载消歧与通配符为主)、`invalid method reference`(fastjson2 33→26→**1**: 三块已治本——(A) `::new_` 渲染修复; (B) **泛型类构造器方法引用实参**——`new ObjectReaderImplFromString(Duration.class, Duration::parse)` 经类字面量去 `(Class)` 造型 + 泛型类补菱形 `<>` 恢复推断, 见 §2, 清 ObjectReaderBaseModule 的 Duration/URI/Charset/Pattern/ZoneOffset/ZoneId/TimeZone 7 行; (C) **本轮: 方法引用赋给原始 `Function` 局部**——`var11_1 = Collections::synchronizedList` 一族, 方法引用值类型从 invokedynamic instantiatedMethodType 上行为 `Function<List, List>` 使局部声明参数化, 见 §2, 清 ObjectReaderImpl{List,ListStr,Map,MapMultiValueType} 共 25 行; **残余仅 1** = `ObjectReaderCreator` 的 `new FieldReaderStackTrace(..., Throwable::setStackTrace)`——构造器实参位的方法引用对非泛型构造器形参(`BiConsumer`)绑定失败, 属构造器实参 functional-interface 目标类型一类, 留下一轮)、`abstract method not overridden`(guava 6)、~~`an enum annotation value must be an enum constant`(guava 3)~~ **已治本(注解值外部嵌套枚举改点号源名, 见 §2; 连带非法 import 错共 -9)**、~~`Enum cannot be converted to <ConcreteEnum>`(guava 3, `Enum<E>.compareTo(E)` 实参多余 `(Enum)` 上行造型)~~ **已治本(JDK 伴生擦除型类型变量抑制 `jdkCalleeParamIsErasedTypeVar`, 见 §2; guava 206→203, 清 `AbstractService$HasReachedRunningGuard`/`$IsStoppableGuard`/`ServiceManager$ServiceManagerState` 3 单元)**、~~`constructor ... has private access`(guava 3, 合成 access-bridge 构造器空体丢 `this()` 委派)~~ **本轮已治本(`writeBlock` 对 marker-only 合成桥补回 `this();`, 见 §2; guava 203→200, 清 `AbstractFuture$UnsafeAtomicHelper`/`$SynchronizedHelper`/`AggregateFutureState$SynchronizedAtomicHelper` 3 单元)** 等。逐个按 [`HARNESS.md`](../HARNESS.md) 流程清零。
   - **`bad type in conditional expression`(guava 11)分桶结论**: 经复现, 这桶并非「LUB 算窄」单根因, 而是**泛型擦除在三元臂里**——`Optional.of(var1.next())`(`next()` 擦成 Object 需 `(E)`)、`ImmutableSet.of().iterator()`(泛型工厂缺 `<E>` 见证)、`Cut.belowAll()`(泛型静态方法推断)等, 与 §3.1 同源, 须在三元臂接入既有实参/见证造型, 留作泛型擦除长尾。`no suitable method found for binarySearch`(guava 7)同理为通配符捕获 `CAP#` + 原始 `(List)` 造型破坏推断, 属 §3.1(b) 难 case。

---

## 4. iso 口径的已知假阳性(**不是缺陷**, 不要去"修")

iso 把每个扁平单元单独编译, 以下失败是方法学产物, 在 tree(重打包)口径下不存在:

- `cannot find symbol: class Outer$Inner` — 扁平 `$` 类型引用对着原始 jar 解析不到(jar 里是源名 `Outer.Inner` 的嵌套类)。codec 30/38、fastjson2 281/342、guava 568/1149 的 iso 失败几乎全是这一类。
- `X has private/protected access in Y` — 内部类合法访问外层私有/保护成员, 单文件编译看不到同编译单元豁免。
- `cannot access X` — 需要兄弟类字节码而 iso classpath 未含其反编译产物。
- **`程序包sun.misc不存在` / `cannot find symbol: class Unsafe`(`sun.misc.Unsafe`, 连 tree 口径也中招)** — harness 用 `javac --release 8`, 其 `ct.sym` 只暴露 JDK 8 受支持 API, **不含 `sun.*` 内部包**。`guava` 里忠实反编译出 `sun.misc.Unsafe` 的类(`Striped64`/`UnsafeByteArray`/`UnsafeAtomicHelper`/`UnsafeComparator` 等, 约 **45** 行 `cannot find symbol`)因此编不过。这是 **`--release` 编译模式的环境产物, 不是反编译缺陷**(任何忠实反编译器同样过不了; 裸 `javac` 仅警告), 真实重打包用含 `sun.misc` 的 JDK 即可。计入 guava 478 的口径里但**不应算作可治本缺陷**。

> 因此 iso 数**不是**北极星指标; 它只用来侧写"哪些类涉及跨类引用"。真正口径永远是 tree。

---

## 5. kill-switch 索引

| 开关(置位即关闭对应治本) | 作用域 |
|---|---|
| `JDEC_GENERIC_PARAM_INFER_OFF` | JDK 泛型方法实参的接收者参数化造型(全部) |
| `JDEC_GENERIC_PARAM_FIELD_OFF` | 仅关字段接收者旁路(从字段 Signature 还原类型参数) |
| `JDEC_GENERIC_SELFMETHOD_PARAM_OFF` | 同类自有泛型方法实参造型(从方法 Signature 还原类作用域类型变量, 公有+私有) |
| `JDEC_GENERIC_SELFMETHOD_PRIVATE_OFF` | 仅关私有同类方法(invokespecial 且目标类==当前类)的扩展, 恢复旧的 invokespecial 一刀切 |
| `JDEC_GENERIC_SUPER_METHOD_OFF` | 继承超类型泛型方法签名并入(this 接收者, 恒等映射一层, 跨类 resolver 加载) |
| `JDEC_GENERIC_RESOLVE_OFF` | 统一跨类泛型解析器(沿接收者泛型超类型层级 DFS + 类型实参替换 σ, 覆盖非-this/非恒等/深链; 附加式兜底, 关闭后回退到上面的特例拼盘) |
| `JDEC_INNER_TYPEVAR_BOUND_OFF` | 扁平内部类注入的外层类型变量的 bound 重建(沿 `$` 链取外层类真实 bound 成 `<C extends Comparable<?>>`; 关闭后回退裸 `<C>`) |
| `JDEC_INNER_ENCLOSING_ARITY_OFF` | 扁平内部类外层形参元数对齐(用量子集 ⊆ 最近泛型外层类形参集时, 整体采用该外层类完整有序形参, 使声明元数对齐引用点 `<K,V>`; 关闭后回退用量子集声明, 复现 `does not take parameters` / `wrong number of type arguments`) |
| `JDEC_NO_ERASED_TYPEVAR_NOCAST` | 擦除型类型变量的多余 upcast 抑制(被调形参是其自身/声明类的形式类型变量时, 丢弃 `(Bound)` 无操作造型以保推断; 关闭后回归旧的擦除-bound 造型) |
| `JDEC_ENUM_COMPARETO_NOCAST_OFF` | 上一项的 JDK 伴生: `Enum<E>.compareTo(E)` 实参不再补 `(Enum)` 上行造型(描述符键控: 方法 `compareTo`/单参/形参 0 为 `java.lang.Enum`; 关闭后回退造型, 复现 `Enum cannot be converted to <ConcreteEnum>`) |
| `JDEC_SYN_BRIDGE_THIS_OFF` | 合成 marker-only access-bridge 构造器空体补回 `this();` 委派(关闭后回退空体, 复现子类隐式 `super()` 落到父类私有无参 → `constructor ... has private access`) |
| `JDEC_CLASSLIT_ARG_NOCAST_OFF` | 类字面量实参对 `Class` 形参不补 `(Class)` 造型(`X.class` 本就是 `Class<X>`; 关闭后回退造型, 复现 `(Class)(X.class)` 擦除泛型) |
| `JDEC_CTOR_DIAMOND_OFF` | 泛型类 `new` 带方法引用/lambda 实参时补菱形 `<>`(关闭后回退 RAW `new`, 复现 `Function<String,T>` 擦成原始 `Function` → `invalid method reference`) |
| `JDEC_METHODREF_INSTANTIATED_TYPE_OFF` | 方法引用值类型从 invokedynamic instantiatedMethodType 上行为参数化 functional interface(`Function<List,List>`, 照搬内联 lambda 分支; 关闭后回退 RAW `Function`, 复现 `var = Collections::synchronizedList` 的 `invalid method reference`) |
| `JDEC_LAMBDA_IMPLICIT_UNUSED_PARAM_OFF` | 未被使用的 lambda 形参隐式渲染(全部形参皆未在体内使用时去掉显式类型 `(Integer l0)`→`(l0)`, 让 javac 从目标 functional interface 推断; 关闭后回退显式类型, 复现裸 `Map.computeIfAbsent` 的 `incompatible parameter types in lambda expression`) |
| `JDEC_BOOL_PARAM_REASSIGN_MERGE_OFF` | 布尔形参分支重赋 phi 合并(`boolean` 形参在分支被 int-0/1 重赋、汇合处读取时续用形参而非分裂出 int 量; 经 `slotStoreReachesParamPhiLoad` 证形参臂+store 臂双臂 phi 才触发; 关闭后复现 `ObjectWriterProvider` 的 `bad operand types: String != int` 槽撞名) |
| `JDEC_REF_SLOT_SIBLING_ARM_MERGE_OFF` | 兄弟臂引用 phi 合并(同槽两不相交臂存兄弟引用类型——`Method`/`Field` 等都 `<: Member` 但互无子类型——汇合处多态使用时, 经 `CommonSuperType` 非 Object JDK 表上界 L + `slotDefPhiReachesLoad` 双证后把 current 加宽到 L 并续用, 旁路 `AssignVarGuarded` 的按类型拆分; `CommonSuperType` 对只共 Object 的 Map/List/Number/String 派发型复用与非 JDK 表类型返回 nil; 关闭后回退拆分, 复现 `FieldWriter.compareTo` 的 `Field cannot be converted to Method` 等 16 行) |
| `JDEC_BOOL_RETURN_SLOT_SPLIT_OFF` | 布尔方法 `return false/true` 复用不相交 int 槽时拆分(槽 current 是纯 int 局部、存储 int-0/1、方法返回 boolean、本 store 经 `slotStoreFeedsReturn` 喂给 ireturn 且经 `slotStoreDisjointFromCurrentWeb` 证 current def 不在本 store 的到达定义 web 时, 把 0/1 重定型为 boolean 令 `AssignVarGuarded` 新铸 boolean 量; 关闭后回退合并, 复现 `LocalCache$Segment` 的 int↔boolean 互转错) |
| `JDEC_BOOL_FIELD_SLOT_SPLIT_OFF` | 上一项的字段-sink 姊妹: boolean 标志(存入 `Z` 字段)复用不相交 int 循环计数器槽时拆分(槽 current 是纯 int 局部、存储 int-0/1、本 store 经 `slotStoreFeedsBooleanField` 前向流入 `putfield/putstatic` 到 boolean(`Z`)字段、经 `!slotDefPhiReachesLoad` 证与 current def 不共达任一 load(真不相交)、且经 `slotStoreDisjointFromCurrentWeb` 证 web 不相交时, 把 0/1 重定型为 boolean 拆分; phi 守门把这与「同一 boolean 变量 try/catch 两臂汇入同一 `putstatic Z`」严格区分后者不拆; 关闭后回退合并, 复现 `JSONPathTypedMultiIndexes.<init>` 的 `bad operand '<'`/`boolean→int`/`boolean++` 共 4 行) |
| `JDEC_LAMBDA_CTX_RESTORE_OFF` | 内联 lambda 体懒解析后还原外围方法 FuncCtx(`DumpClassLambdaMethod` 闭包在递归 lambda dump 前后保存/还原 `FuncCtx.FunctionName/FunctionType/IsStatic/TypeParams` + `MethodType/CurrentMethod`, 防止 lambda 的 void 上下文泄漏到外围方法 invokedynamic 之后的字节码; 关闭后还原跳过, 复现 `BeanUtils.isWriteEnumAsJavaBean` 的 `return 1/0` 未转 `true/false`) |
| `JDEC_INSTANCEOF_REPLACEVAR_OFF` | `OP_INSTANCEOF` 的 CustomValue 转发 ReplaceVar 给捕获的操作数(像 `OP_CHECKCAST` 那样, 使 disjoint 槽末期身份重绑能到达 `instanceof` 操作数; 关闭后操作数身份冻结, 复现 `JSONSchema.of` 的 `var6 instanceof Integer`(var6 实为 int)→ `unexpected type / found: int`) |
| `JDEC_COVER_UNDECLARED_OFF` | 同槽拆出两个同名 `varN` 之一无声明时的名字制作用域覆盖安全网(`coverUndeclaredGeneratedLocals` 在 `RewriteVar` 末, 当某 `varN` 子树里有未被同名声明词法覆盖的出现——含 if/loop 条件 head 内嵌赋值/读取——且类型唯一时, 把已存在的同名声明扩到覆盖所有出现的 LCA 块顶; 关闭后跳过, 复现 `ObjectWriterAdapter.writeWithFilter` 的 var19/var21、`JSONReaderUTF16/UTF8.readOffsetDateTime` 的 var7 等 `cannot find symbol: variable varN`) |
| `JDEC_NULLINIT_NARROW_OFF` | 孤儿/Object 生成局部按 AST reassignment 类型恢复声明类型(`narrowNullInitObjectDecl` 在 `RewriteVar` 末, 当某 `varN` 的非 null reassignment 类型恰为单一非 Object 引用类型 S 且该名仅作接收者/赋值目标使用时: 已有 `Object varN` 声明→收窄到 S; 完全无声明的孤儿→方法首部合成 `S varN;`; 关闭后跳过, 复现 `ObjectWriterAdapter.writeWithFilter` 的 `var30.write(...)` 因 dumper 兜底默认 `Object var30 = null` 而 `cannot find symbol: method write, location: variable var30 of type Object`) |
| `JDEC_ORPHAN_GLOBAL_REBIND_OFF` | 跨作用域孤儿读全方法唯一重放(`Scope.allReplace` 累积各作用域 `(oldId→newId)` 重绑, `rewriteVar` 递归结束后 `replayUnambiguousRebindings` 对**唯一映射**的 oldId 做一次全方法 `ReplaceVar`, 把落在兄弟作用域、未被任一切片 `idReplaceMap` 覆盖的孤儿读补绑到其 store 的声明 id; 不相交槽真分裂——oldId→多个不同 newId——严格跳过; 关闭后跳过补绑, 复现 `JSONPathParser.parseFilter` 的 `(String)(SizeFunction)` 即孤儿读裸 `var11` 撞名 `SizeFunction var11` 声明 → 16 行 `cannot be converted to String`) |
| `JDEC_REF_SLOT_OBJECT_SUPERTYPE_ARM_MERGE_OFF` | disjoint 槽族「Object 超类臂合并」(`reachingRefSlotObjectArmMerge`, shape A 单向): 槽 current 类型恰为 `java.lang.Object`、某不相交臂存更具体非 Object 引用、`slotDefPhiReachesLoad` 证 phi 时**续用 current(Object)**, 使具体重赋臂成 Object 变量的普通赋值, 汇合处 `(Map)`/`(Collection)` 等造型读落到 Object 上而合法。绝不改任何声明类型(不做对称的「具体加宽到 Object」, 该 shape B 实测回归); 关闭后复现 `ObjectWriterAdapter.toJSONObject` 的 `String cannot be converted to Map/Collection`(3 行)+ `JSONPathTwoSegment`(2 行)) |
| `JDEC_REF_SLOT_ARRAY_COVARIANT_ARM_MERGE_OFF` | disjoint 槽族「数组协变父臂合并」(`reachingRefSlotArrayCovariantArmMerge`, 上一项的数组元素伴生): 同槽两不相交臂存等维(dim==1)引用数组(`new String[]` / `new Object[]`), 元素类型串不同、经 `arrayElemLUB`(Object 万能超类 / jar 内 `CrossClassDirectLUB` / JDK `CommonSuperType`)求出元素 LUB、`slotDefPhiReachesLoad` 证 phi 时把 current **加宽到元素 LUB 数组**(`String[]→Object[]`)并续用, 两臂成同一 `Object[]` 变量的普通再赋值。**刻意不门控 `IsNullInitialized`**(CSV current 是 null-adopted ref, 由「已是数组类型」隐式保证非标量 Object null-init); 既有 `reachingRefSlot*` 四族因 `classNameOf` 对数组返 false 而全 bail。关闭后回退拆分, 复现 `CSVReaderUTF8/UTF16.readLineValues` 的 `Object cannot be converted to String`(3 行)) |
| `JDEC_TYPEVAR_ARRAY_ELEM_STORE_CAST_OFF` | 跨库「Object→类型变量造型缺失」族·LHS 数组元素存储(`typeVarArrayElementStoreCast`, `typeVarFieldStoreCast` 的数组元素姊妹): 同类字段数组元素存储 `this.buf[i] = objExpr`(字段声明 `T[]`/`K[]`/`V[][]`)漏补 `(T)`——元素类型变量取自字段已录 Signature(`FieldTypeVar`, 与声明逐字一致), 索引维数==字段数组维度、值非 null 非基元且渲染类型≠该变量时补 `(T) value`(引用数组元素的合法 unchecked cast, aastore 擦除为 no-op)。仅限同类字段数组基、维数严格匹配(部分索引留数组型变量不补标量)。关闭后回退裸存储, 复现 `TopKSelector.buffer[i]`/`HashBiMap.keys[i],values[i]`/`DenseImmutableTable.values[r][c]`/`ImmutableSortedMultiset$SerializedForm.elements[i]` 的 `Object cannot be converted to T/K/V/E`(6 行) |
| `JDEC_TYPEVAR_ARRAY_ARG_CAST_OFF` | 跨库「Object→类型变量造型缺失」族·数组实参闸(`typeVarArrayArgCast`): 泛型解析器已把调用形参复原为「作用域内可命名类型变量 T」(`resolvedGeneric && IsTypeParam`)但实参静态类型是引用数组时, 补出 `m((T) arr)`——类-类造型分支(`ok1&&ok2`)因数组 `RawType()` 是 `*JavaArrayType` 非 `*JavaClass` 而永不触发。引用数组绝不可直赋裸类型变量, 源必带 unchecked `(T)`(字节码擦除 no-op)。仅在形参确为裸类型变量(`ok1`, 若 T 实例化为数组则不触发)时补。关闭后回退裸实参, 复现 `CSVReaderUTF8/UTF16.readLineObjectAll` 的 `consumer.accept(values)` → `Object[] cannot be converted to T`(2 行)。**配套缺口①**(`Comparator.compare` 纳入 `jdkMethodParamTypeArgIndex`)在伞柄 `JDEC_GENERIC_PARAM_INFER_OFF` 下——形参解析回 T 后补 `compare((T)a,(T)b)`, 复现 guava `Comparators.isInOrder/isInStrictOrder` 的 `Object cannot be converted to T`(2 行) |
| `JDEC_CTOR_METHODREF_FIX_OFF` | 构造器方法引用 `::new` 渲染(关闭后回归 `SafeIdentifier` 误清洗出的 `::new_`) |
| `JDEC_RETURN_DECL_FIX_OFF` | 返回-嵌入赋值局部的声明合成(跳过 `return varN` 伪声明 + 以方法返回类型声明) |
| `JDEC_OBJECT_RET_DOWNCAST_OFF` | 返回点 Object→具体引用类型 向下造型 |
| `JDEC_THIS_REPARAM_CAST_OFF` | `cast()` 重参数化返回点造型(同擦除 `return this` 且返回类型实参≠类自身参数时补 `(C<N1>) this`; 关闭后回退裸 `return this;`) |
| `JDEC_DUP_MULTI_TEMP_SPLICE_OFF` | dup/dup2 多临时拼接(复合数组自增同时物化数组+下标时, 把同 `node.Id` 的孤儿赋值节点重新拼回主节点之前; 关闭后回退到丢弃孤儿, 复现 `varN[varN]`) |
| `JDEC_WILDCARD_RET_CAST_OFF` | 通配符返回造型(同类 `this.` 调用返回 `R<?>` 而方法声明返回 `R<T>` 时补 `(R<T>)`; 关闭后回退裸返回) |
| `JDEC_WILDCARD_FIELD_CAST_OFF` | 通配符参数化字段存储造型(同类字段 `Class<? super T>` 存入同擦除 `Class<?>` 时补 `(Class<? super T>)`; 关闭后回退裸存储) |
| `JDEC_NULL_DUP_FOLD_OFF` | null 链式字段赋值临时抑制(裸 `null` 不折共享 `Object varN`, 每存储点重新物化; 关闭后回退 `Object varN = null` 临时, 复现 `Object cannot be converted to Node`) |
| `JDEC_BOOL_SIBLING_ARM_MERGE_OFF` | try/catch 兄弟臂 boolean phi 合并(第二臂 int-0/1 存储延续已 boolean 的 current 版本, phi 共享 load 证明同变量; 关闭后回退分裂, 复现 `SqlTypesSupport` 的 `Object var1 = null` → `Object cannot be converted to boolean`) |
| `JDEC_BOOL_TO_INT_COERCE_OFF` | 内在布尔值赋 int 缺 `? 1 : 0` 造型(左值为 int 且右值是内在布尔——布尔连接/双臂布尔三元, 非裸 ref——时包成 `(rhs) ? 1 : 0`; 关闭后回退裸赋值, 复现 `DoubleMath`/`LongMath`/`ImmutableSortedMap` 的 `boolean cannot be converted to int`) |
| `JDEC_ANNO_ENUM_NESTED_DOT_OFF` | 注解值里外部嵌套枚举常量改点号源名(嵌套枚举且 `foldSiblingResolver` miss = 外部依赖时, `Outer$Inner.CONST` → `Outer.Inner.CONST` 并 import 外层; 关闭后回退扁平 `$` 名, 复现 `an enum annotation value must be an enum constant`) |
| `JDEC_GENERIC_SUPERWILDCARD_OFF` | `? super E` 消费者实参造型(下界通配符接收者实参取其下界类型变量作 `(E)` 造型; 关闭后回退遇通配符一律 bail) |
| `JDEC_CTOR_WILDCARD_CAST_OFF` | `NATURAL_ORDER` 构造器自调用通配符实参造型(`this(...)` 自调用裸类型同但参数化不同时补造型, 带偏移护栏 + `IsThis` 护栏; 关闭后回退裸实参) |
| `JDEC_SWITCH_NONDOM_MERGE_BREAK_OFF` | switch 合并点的非支配前驱不插 break(仅 switch 传递支配的 case 体前驱才改写成 break, switch 外的 if/else-if 链共享 merge 边保留 fall-through; 关闭后回退给所有前驱插 break, 复现 `break outside switch or loop`) |
| `JDEC_POP_ELIDE_OFF` | pop/pop2 裸值语句 elide |
| `JDEC_NO_ENUM_SWITCH_FOLD` | enum-switch `$SwitchMap` 折回 |
| `JDEC_NO_ENUM_FOLD` | enum 常量体子类内联 |
| `JDEC_LIVEINTERVAL_OFF` / `JDEC_LIVEINTERVAL_WEB` | 活跃区间声明摆放 / web 复用 |
| `JDEC_TYPELUB_OFF` / `JDEC_TERNARY_DECL_LUB_OFF` | 类型 LUB(含反射家族 Method/Field/Constructor→Member 行) / 三元声明点 LUB |
| `JDEC_TERNARY_DECL_LUB_CACHE_OFF` | 三元声明加宽时同步刷新三元 cachedType(关闭后只重置 ref, 复现声明印陈旧窄类型 `Method var1`) |
| `JDEC_TERNARY_DECL_LUB_CROSS_OFF` | 跨类(jar 内)三元 LUB 加宽(`ternaryDeclLUBCrossClass` 经 `ClassContext.SiblingSuperTypes` 解析 jar 内 RAW 父类型, 两臂存在直接传递子类型关系时把声明 ref 加宽到上界臂; 不受 `isFirst` 闸门约束以覆盖 null-init 后赋值; **仅 ResetVarType, 不调 SetCachedType** 以免回流改窄以参数/this 为臂的 `?:` 实参; WIDEN-ONLY + provider 非空双闸门; 关闭后回退, 复现 `AllOf`/`ArraySchema`/`ObjectSchema` 的 `Any.INSTANCE : Any.NOT_ANY` 三元 `bad type in conditional expression`) |
| `JDEC_GENERIC_INFER_OFF` | JDK 泛型方法返回实例化 |
| `JDEC_NESTED_PUBLIC_OFF` | 嵌套类型 public 复原 |

> 更多底层开关(`JDEC_SLOT_*` / `JDEC_*REACHING*` / `JDEC_IF*` / `JDEC_TRY_*` / `JDEC_LOOP_*` 等)散落在 `decompiler/` 源码中, 用 `rg 'os.Getenv\("JDEC_' classparser/decompiler` 列全。

---

## 6. 验收红线(每轮治本必须全满足)

1. 选靶用 tree inventory, 单点根因, 不打过用例补丁。
2. 复杂改动配 `JDEC_*` kill-switch; A/B delta(`OFF-ON`)对**所有** 4 jar 必 ≥ 0(本 jar 降、它 jar 不升)。
3. 配承重测试(ON/OFF 断言)+ 回归种子(`testdata/regression/*.class` + `.golden`)。
4. 全量 `go test ./...` 30s 内全绿; 确定性测试不得变红。
5. 安全契约: 永不输出不可解析的 Java; 宁可带标记 stub 也不输出"看似对实则错"; 不 panic。
